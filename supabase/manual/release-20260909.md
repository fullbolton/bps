# 035 — Supabase ve Vercel production yayını tamamlandı

2026-09-09. BPS projesi: `dffdzbmnmnokbftbujsy`.

## Güncel durum

**20260909000100–20260909002700: 27/27 migration canlıda uygulandı. Bekleyen paket migration'ı yok.**

Son kullanıcı “devam” yanıtı, açıkça anlatılmış auth.sessions oturum iptali, yönetici rol/üyelik koruması ve davet/kayıt etkilerini onayladı. Son üç migration (001600,002300,002400) aynı MCP onay kanalından başarıyla geçti. Mevcut kullanıcıların oturumları topluca silinmedi; bu kurallar sonraki ilgili işlemlerde çalışır.

Canlı ledger'daki 27 SQL metninin SHA256 değeri yerel dosyalarla eşit. MCP uygulama zamanına göre oluşan sürümler, yalnız metin eşleşmesi ve çakışma kontrolünden sonra özgün dosya sürümlerine uzlaştırıldı. Orijinal MCP sürümleri `release-20260909.json` içinde korunur. Uygulanmayan hiçbir migration uygulanmış işaretlenmedi.

Eski `20260722000200_role_expand_asistan.sql` bu paketin parçası değildir; tamamlanmamış eski rol taslağını toplu db push ile uygulamayın. Migration dosyalarındaki NOT APPLIED yorumları yazıldıkları dönemin kaydıdır; hash korunması için değiştirilmedi. Güncel durumun kaynağı bu rapor, JSON manifest ve canlı ledger'dır.

## İşe Başlama Takibi

`/talepler/ise-baslama` gerçek atamalarla kalıcı çalışır. Başlangıç saati, sorumlu ve arama takvimi; arama sonucu/olay zamanı/not/ETA; şube veya saha teyidi; gerekçeli düzeltme; 3 dakikalık arama sahipliği; komut tekrarı ve revizyon koruması vardır.

Personelin “şubedeyim” beyanı bağımsız teyit sayılmaz. Teyit ve günlük attendance=present aynı transaction'dır. Teyidi geri alma gerekçelidir; attendance unreported olur. Eski attendance arayüzü teyitli kaydı sessizce absent yapamaz. Yedek atama önceki atamanın planını devralır, kişinin görüşme ve teyit geçmişi ayrı kalır. Kaldırılmış/iptal edilmiş atamaya yeni takip kaydı yazılamaz. Eski kayıtlara varsayılan saat uydurulmaz.

Takip planı ve olay tablolarının ham yazma yetkisi istemcilere kapalı; tenant/rol doğrulamalı RPC kullanılır. Tarayıcı yalnız komut hash/kimliğini kurtarma amacıyla tutar; esas kayıt Supabase'dedir.

## Kabul kanıtı

- Yerel kabul: **23/23** genel/SQL/TypeScript/build kontrolü, **145 operasyon unit**. Rapor: `/var/folders/fg/qm_gg6w16299dhr_lz9xr38r0000gn/T/bps-acceptance-Riv0gS/report.md`. Uygulanan SQLler bu testlerden geçen dosyalarla aynıdır.
- Yeni takip: 16 PostgreSQL kontrolü; scope, rol/üyelik, ham erişim, geçersiz zaman/plan, arama sahipliği, beyan/teyit ayrımı, attendance tutarlılığı, tekrar, eşzamanlı revizyon ve yedek atama.
- Gerçek yerel Auth/RPC: plan→arama→teyit; yeni oturumdan aynı komut tekrarlandığında tek teyit ve present kaldı. Geçici kabul hesabı ve kayıtları temizlendi.
- Yerel tarayıcı: mevcut sentetik atamada saat/sorumlu→personel beyanı (teyit bekler)→Sentetik Şube Yetkilisi teyidi→sayfa yenileme sonrası 3 olay ve teyitli durum korundu.
- Canlıda READ ONLY transaction ve gerçek üyelikten üretilen claim bağlamında membership, start board, workspace_setup, daily_dashboard, dashboard_activity ve list_workspace_invitations okuma kontrolleri geçti. Geçersiz davet false döndü. Bu kontrol production Auth token/browser yazma smoke'u değildir.
- Canlı izin/nesne ölçümü: üç görev koruma trigger'ı açık; auth.users kayıt trigger'ı1; admin owner postgres; admin/davet yönetimi/profil hazırlama anon=false; davet uygunluk okuması anon=true (giriş öncesi tasarım gereği); ham davet INSERT=false; ilgili yeni tablolarda eksik RLS0; refresh_tokens→sessions cascade mevcut.
- 4 mevcut görev için 4 baseline geçmiş satırı ölçüldü; mizan_uploads0 ve financial_summaries0. Luca eski derive authenticated=false/raw INSERT=false; atomik RPC authenticated=true/anon=false.

## Vercel yayını ve doğrulama — 2026-09-09

Kullanıcının “vercel üzerinden yayın yapabilirsin” talimatıyla yeni frontend production'a yayınlandı ve alan adına geçirildi. Canlı adres: https://www.bpsys.net/talepler/ise-baslama (`bpsys.net` bu alan adına yönlenir).

- Vercel proje `bps`; yeni deployment `dpl_7dGwr1wHZc2REPBUYuJwjNnE6RZk`, immutable URL https://bps-2lv5ht9i5-furkanyahsi-1537s-projects.vercel.app. Önce Ready doğrulandı, sonra promote edildi; alan adı inspect ve canlı health yanıtı yeni kimliği doğruladı.
- Önceki production: `dpl_7y1Y5dgjG34sNKABRFmWDZDtUaK2`, https://bps-pta7208eo-furkanyahsi-1537s-projects.vercel.app. Bu yalnız frontend geri dönüş referansıdır; 27 migration geri alınmış olmaz, eski Luca istemcisi atomik RPC ile uyumlu olmayabilir.
- Üretim Supabase hedefi `dffdzbmnmnokbftbujsy`. Operasyon bayrağı üretime eklendi; canlı server-render edilmiş takip ekranı ve Dashboard operasyon bölümü bayrağın etkin olduğunu kanıtladı. E-posta gönderimi açılmadı; cron değiştirilmedi.
- Git push/commit yapılmadı. 246 uygulama dosyası (src/public ve gereken build yapılandırması) temiz staging dizininden gönderildi. Yerel env dosyaları, yedekler ve iç defterler gönderilmedi. Kaynak snapshot SHA256 listesi `release-20260909-source-sha256.json` içinde.
- Yayın öncesi 5 kod kontrolü geçti; ilk build Google Fonts DNS nedeniyle başarısız oldu. Ağ izinli izole build başarılı, Vercel production build de başarılı. Önceki 23/23 SQL/genel kabul ve 145 unit kanıtı korunuyor.
- 18:13 UTC health: service_role_jwt, anon_jwt, service_role_query, cron_secret_present ve resend_key_format **5/5 PASS**; HTTP 200 ve deployment kimliği eşit. Anahtarsız health 401, kimliksiz takip sayfası login/returnTo yönlendirmesi, login 200.
- Mevcut production tarayıcı oturumuyla takip ve Dashboard veri okuması başarılı. Bugün 0 atama; operasyon ve son aktiviteler boş durumları hatasız. Üretimde test kaydı oluşturulmadı. Bu kanıt üretimde arama/teyit yazma kabulü değildir.
- İlk CLI env kontrolü repo `.env.local` dosyasının overlay etkisiyle yanlış proje gösterdi; temiz staging içinde tekrar gerçek production hedefi doğrulandı. İlk deploy otomatik onay reddi bu bulgu ve build kanıtı tamamlandıktan sonra aynı izin kanalında çözüldü. Son env-export kontrolünde operasyon bayrağı boş döndü; indirilmiş değer bayrak kanıtı olarak kullanılmadı, canlı render koşulu doğrulandı.

## Sınır ve sıradaki kabul

**localhost:3000 dedicated yerel sentetik Supabase'e bağlı kalır; canlı uygulama yukarıdaki production adresindedir.** Paket migration'ı ve frontend yayını beklemiyor. Gerçek bir günlük atamayla plan→arama→şube/saha teyidi→yenileme sonrası kalıcılık production kabulü açık. Gerçek operasyon kaydı olmadan tamamlandı olarak işaretleme; başka modüle geçmeden bu kabulü ele al.

Takip listesi50atama/sayfa; mevcut arama/aksiyon/sorumlu filtreleri sayfa içinde çalışır ve bu UI'da belirtilir. Portföy genelinde filtre/özet, otomatik SMS/arama/push ve GPS/NFC varış doğrulaması bu sürümde yoktur. İnsan tarafından alınan şube/saha teyidi kayıt altına alınır.

## Geçiş geçmişi

032: 13/27 uygulandı, mevcut tablo etkileri için otomatik denetim ayrı onay istedi. 033: kullanıcı devam onayıyla24/27; 001600/002300/002400 için oturum/rol etkileri açıkça soruldu. 034: son devam onayıyla27/27 ve canlı doğrulamalar tamamlandı. Retler aynı onay kanalında çözüldü; etraflarından dolaşılmadı. Önceki defter notlarındaki “bekliyor” ifadeleri tarihseldir.

035: Kullanıcının açık Vercel yayın onayıyla temiz kaynak snapshot yayını ve production promote tamamlandı; canlı health ve kimlikli okuma doğrulandı.
