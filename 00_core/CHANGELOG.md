# CHANGELOG.md

> **044 — Yerel render yükü düzeltildi; yayında değil.** 100 personelli haftalık PDF devam satırlarında tam ekran listesinin gereksiz kopyaları kaldırıldı. HTML ad tekrarları 1.000→200; gömülü CSS dahil boyut 145.473→112.417 byte. Ekran/PDF 100 isim kabulü, TypeScript ve 043 ile toplam 10 PDF sayfasında piksel eşitliği geçti. SQL/veri/push/deploy yok; canlı 043 korunuyor. [Kanıt](../01_product/HAFTALIK_CIKTI_RENDER_YUKU.md).

> **043 — 2026-09-10: Haftalık PDF düzeni yayında.** Metin sütunları genişletildi; özetler tek satırda. Uzun personel listeleri yazdırmada 12 kişilik devam satırlarına ayrılıyor; şube/gün tekrarlanıyor, sayılar yalnız ilk satırda. Sentetik Chromium PDF kabulü: tek talep 1 sayfa, 40 talep 5 sayfa (önce 6), 100 personel 4 sayfa; kimlikler eksiksiz ve birer kez. Kaynak `b2c921b`; canlı `dpl_3496nCsZVRrsykaWEiCGJeKAdzyk`; build, sağlık 5/5 ve canlı haftalık ekran geçti. Yeni SQL/veri yok. Native yazdırma diyalogu ve canlı CSV byte kontrolü erişim engeli nedeniyle açık. [Kanıt](../01_product/HAFTALIK_CIKTI_KABULU.md).

> **042 — 2026-09-10: Aktivite olay adları yayında.** İşe başlama planı, arama sonucu, bağımsız teyit, yeniden açma ve geçici arama üstlenme/bırakma ayrı başlıklara sahip. Yeni SQL yok. `0446684` push edildi; canlı `dpl_5wGgUBaofM2qMH3nCK9gMnpMzUwU`. İki mevcut test, TypeScript, production build, sağlık 5/5 ve kimlikli Dashboard okuma geçti. Mevcut canlı listede takip olayı olmadığından altı yeni başlık o listede ayrıca gözlenmedi; yeni test verisi oluşturulmadı. [Kanıt](../01_product/AKTIVITE_ISE_BASLAMA_ADLARI.md).

> **041 — 2026-09-10: Blok 1 yayında, çekirdek canlı kabul tamam.** Üç bekleyen SQL uygulandı; bu teslimin 30/30 migration kaynağı ve ledger sürümü doğrulandı. `565059e` kaynakları `codex/block-01-release` dalına push edildi; main birleştirilmedi. Vercel `dpl_DZvtjhLdoishravp1YYwJnM9Uuoq` www.bpsys.net üzerinde canlı, sağlık 5/5. Kimlikli sentetik aday firma → şube → personel → talep → atama → plan → arama → bağımsız teyit → reload ve Dashboard geçti. Bu tur test verileri temizlendi; eski kayıtlar korundu. CSV gerçek HTTP kabulü yerel 9/9; canlı CSV byte/PDF sayfalama kabulü ayrıca açık. Önceki bekliyor/yayınlanmadı kayıtları tarihseldir. [Tek teslim notu](../01_product/BLOK_01_TESLIM.md).

> **040 — Blok 1 yerel teslim paketi hazır.** Haftalık CSV gerçek HTTP 9/9 ve bağımsız okuyucu, tarayıcı toplam/iptal/boş hafta kabulü geçti. Kabul betiği artık kendi geçici hesap/verilerini temizler. Toplu manifest 249 uygulama + 30 SQL + 17 kabul dosyasını doğrular; uygulama 036–039 kabul snapshotlarıyla birebir. Genel 5/5 ve manifest 2 test geçti. Yeni uygulama/SQL değişikliği, production/push/deploy yok. Yayın ve canlı kabul açık; başka modül açılmayacak. [Tek teslim notu](../01_product/BLOK_01_TESLIM.md).

> **039 — Blok 1: aday firma uyumluluğu yerelde tamamlandı.** Aday ve aktif firmalar yeni operasyona uygun; CRM durumu kendiliğinden değişmiyor. Yedi SQL fonksiyonu, firma seçimi ve kurulum sayacı birlikte düzeltildi. 9 native kontrol, 159 unit/genel 5/5, gerçek yerel Auth ile aday firma → şube/CSV → talep → atama → plan/arama/teyit → yeni oturumdan okuma ve build geçti. Geçici veriler temizlendi. Canlı/push/deploy yok; 02800,02900,20260910000100 production’da bekliyor. Blok kapanmadı; sıradaki haftalık CSV kullanıcı kabulü ve birlikte teslim envanteri. [Kanıt](../01_product/ADAY_FIRMA_OPERASYON_UYUMU.md).

> **038 — Fable bulguları yerelde düzeltildi.** Kesin retlerde tek komutun sunucudan uzlaştırılması; plan öncesi manuel görüşme/teyit, saniye hassasiyeti ve dar giriş doğrulaması tamamlandı. 02900 yalnız dedicated yerelde. 157 unit, genel 5/5, yeni 7 SQL kontrolü, Fable yarış/kapsam regresyonları 26/26, gerçek yerel Auth/RPC, tarayıcı ret mesajı ve build geçti. Canlı 035 ve 27 migration değişmedi; 02800/02900 henüz production’da değil. [Düzeltme ve kanıt](../01_product/FABLE_REVIEW_01_CODEX_TRIYAJ.md). Sıradaki iş 036–038 yayın adayının birlikte kabulü; production gerçek atama kabulü açık.

> **037 — Dashboard İşe Başlama özeti yerelde tamam.** Takip bekleyenlerin gerçek toplamı, ilk3kayıt, kontrol zamanı ve aynıgünün aksiyon listesine bağlantı eklendi. YeniSQLyok;02800gerekiyor.151unit/genel5/5,izolebuild,yerel0→1sayaçvefiltrelibağlantı kabulü geçti; geçici kayıtlar temizlendi. Canlı035/27migration değişmedi. Fable sonuç raporu geldi; sıradaki ikiP2vebirP3bulgunun Codex doğrulaması/düzeltmesi. Git yayını `fb1b218`, devir `2b53d98` olarak başka çalışma tarafından commit edilmiş; push bu tur ölçülmedi. [Kanıt](../01_product/DASHBOARD_ISE_BASLAMA_OZETI.md).

> **036 — yerel tamamlandı, canlıya yayınlanmadı.** İşe Başlama Takibi arama/sorumlu/aksiyon filtreleri artık yeni `ops_start_board_filtered` RPC ile tüm gün üzerinde, sayfalama öncesi çalışır. 02800 yalnız dedicated yerelde; canlı035ve27migration korunuyor. 8nativeSQL,gerçek yerelAuth/RPC,genel5/5veizolebuild geçti. Kaynak çalışma ağacı artık035yayın snapshotından farklıdır. [Plan/kanıt](../01_product/ISE_BASLAMA_TUM_GUN_FILTRELERI.md). Gerçek production atama/teyit kabulü hâlâ açık.

> **035 — 2026-09-09: Vercel production yayını tamamlandı.** [İşe Başlama Takibi](https://www.bpsys.net/talepler/ise-baslama) canlı Supabase üzerinde açılıyor. 27/27 migration uygulanmış durumda. Yeni sürüm `dpl_7dGwr1wHZc2REPBUYuJwjNnE6RZk`; production sağlık 5/5 ve mevcut hesapla takip + Dashboard okuma geçti. Bugün atama olmadığı için production arama/teyit yazma kabulü açık; yerel SQL/Auth/RPC/tarayıcı kabulü geçerli. localhost yerel kalır; git push yapılmadı, çalışma ağacının uygulama dosyaları CLI ile yayınlandı. Önceki frontend-bekliyor notları tarihseldir. Kanıt: `supabase/manual/release-20260909.md/json`.

2026-09-09 — 02400: `/kayit` davet kontrollü Auth signup, `/auth/callback` PKCE dönüşü; güvenli başlangıç profili/eksik profil onarımı. Metadata'dan rol/admin/unit kabulü kaldırıldı. Yerel18SQL,AuthAPI,type/build geçti. Mail doğrulaması açık prod ayarıyla uçtan uca kabul henüz yok; gerçek posta gönderilmedi.

2026-09-09 — 02300: Kurulumda mevcut hesaplara davet kodu (7 gün), listeleme/iptal; `/davet` doğrulanmış alıcı hesabıyla kabul. Tek üyelik, rol kontrolü, hash saklama, iptal/kabul yarışı ve refresh oturumu iptali. Otomatik e-posta/yeni hesap açma yok. Önceki Dashboard/kurulum tenant claim alanı varsayımı kaldırıldı, verified RPC kullanılır. Yerel13SQL/AuthAPI/127unit/typebuild geçti.

2026-09-09 — 02200: `/kurulum` yöneticinin firma/şube/ekip/personel/ilk plan/haftalık çıktı adımlarını gerçek sayaçlarla bir araya getirir. Dashboard ve Ayarlar bağlantısı. Tamamlandı yüzdesi yok; read-only verified tenant RPC. Yerel 10SQL + Auth/API + type/build geçti. Sıradaki kullanıcı davetleri.


2026-09-09 — Dashboard: manuel risk kartı ve günlük otel e-postası kaldırıldı. Son aktiviteler, tenant/rol doğrulayan 02100 RPC ile son 20 gerçek operasyon/görev atama/PDF olayını gösterir; yenileme, yükleme, hata ve boş durumları ayrı. Yönetici/operasyon kapsamı; baseline hariç, tam audit/realtime değil. Ayrıntı: `01_product/DASHBOARD_VE_SAAS_SIRASI.md`. Yalnız yerel geliştirme.

## Purpose
This file records meaningful documentation and structural decisions for BPS.
It is a historical record, not a replacement for source-of-truth documents.

---

## Usage Rules
- Log meaningful changes to authoritative documentation.
- Keep entries short and factual.
- One row may cover a grouped batch if the scope is coherent.
- Newest entries should appear at the top.
- Do not use the changelog to define product behavior by itself.
- This file follows one ordering rule only: newest entries go at the top.

Template row:

`| YYYY-MM-DD | file or batch | short change summary | reason / impact |`

---

## Entries

| Date | File or Batch | Change Summary | Reason / Impact |
|---|---|---|---|
| 2026-09-09 | 02000 sözleşme ek protokolleri | Değişmez main/appendix rolü, bağımsız sürüm/komut hedefi, scoped history/path, firma listesi başlığı/bağlantısı;121unit/32native/15API/full18/18 | Ek protokol ana PDF'yi ezmez; eski01900 komutları korunur;20migration yalnız sentetik yerelde; evrak takip sahipliği planlandı |
| 2026-09-09 | 01900 PDF yükleme sürekliliği | İlk/değiştirme kalıcı komut, iptal tombstone, reload/retry, HTTP byte ve Storage readback;115unit/20native/10API ve gerçek browser kabulü | Kayıp yanıt ikinci sürüm üretmez;10MiB middleware kesilmesi düzeltildi;19 migration yalnız sentetik yerel; ek protokol planı sırada |
| 2026-09-09 | PDF sürüm geçmişi | 01800 değişmez referanslar, actor/obje, CAS replace, scoped indirme ve Storage koruması;105unit,18native,10StorageAPI,16/16 paket. | Yalnız yerel; sonraki kalıcı upload komutu/kesintiden devam, ardından ek protokol. |
| 2026-09-09 | Sözleşme yenileme sahipliği | 01700 revision, tek task ilişkisi/receipt; gerçek owner, tarih, dayanak formu.101unit,18native,7API,15/15 paket; browser çift tık1→devir→owner. | Yalnız sentetik yerel; sıradaki PDF sürüm geçmişi. |
| 2026-09-09 | Toplu devir ve kullanıcı ayrılış kapısı | 01500 atomik100 iş/receipt;01600 aktif iş/üyelik/rol guard, eski admin owner/ACL korunur.96unit,26+19 native, API10+7, paket14/14. | İki aşama dedicated yerelde tamamlandı; sıradaki gerçek sözleşme yenileme sahipliği. |
| 2026-09-09 | Firma bağlamından randevu | Mevcut formda hazır firma, scope/reset ve pending koruması; native tarih UI create, çift tıklama1/pasif0/iptal0; hızlı paket+build6/6, API9/9. | SQL değişmedi; sonraki toplu görev devir planı hazır. |
| 2026-09-09 | Atomik randevu/takip tamamlaması | Yeni scoped RPC/receipt, güvenli tekrar ve rollback;85 unit/22 native/9 randevu API, paket12/12; tarayıcı1task. | 01400 yalnız yerel; eski RPC kapalı, yeni randevu tarih UI kabulü ayrı açık. |
| 2026-09-09 | Görev atama geçmişi ve revision | DB geçmiş trigger'ı, tüm uygulama update'lerinde CAS, panel geçmişi/çatışma yenileme;79 unit/16 task native/11 görev API, tam paket11/11. | 01300 yalnız yerel; eski task policy/admin taşıma değişmedi. |
| 2026-09-09 | Günlük talep → mevcut görev formu | Sunucudan doğrulanan firma/başlık önseçimi, form sıfırlama/çift gönderim koruması; 75 unit,10/10 paket,9 yerel görev servis kontrolü ve tarayıcı kaydı. | Kalıcı talep bağı yok; test fixture genişledi, üretim/push/deploy yok. |
| 2026-09-09 | Tek komut yerel kabul | 10 adım passed,11 runner testi; SQL/API/build modları, özel rapor ve geçici build kopyası; eksik runtime exit1. | Dev sunucusu korunur; yeni migration/prod/push/deploy yok. |
| 2026-09-09 | Yönetici dizin aktifliği | Revision/idempotency, korunan geçmiş, lokasyon SHARE kilidi; 68 unit / 181 DB / 48 yarış / 42 API. | 01200 yalnız yerel; operasyon salt-okunur. |
| 2026-09-09 | Aranabilir şube/personel dizini | 50 satır/sayfa, kod arama ve aktif/pasif filtre; 66 unit / 163 DB / 37 API. | 01100 yalnız yerel; aktiflik yazması sonraki dilim. |
| 2026-09-09 | Günlük operasyon kontrol listesi | Aktif gelmedi/açık/bildirim bekleyen; arama/filtre ve hedef kart; 61 unit / 33 API / 180 kaynak static temiz. | SQL değişmedi; yalnız yerel, prod/push/deploy yok. |
| 2026-09-09 | Haftalık gerçekleşme özeti | İptal/kaldırma geçmişi dahil ayrı sayaçlar; 53 unit / 149 DB / 30 API, 177 dosya static temiz. | 01000 yalnız yerel; otomatik devam aktif. |
| 2026-09-09 | Günlük gerçekleşme + atomik personel değişimi | Ayrı gerçekleşme/revision, korunan tarihçe; 48 unit / 141 DB / 37 yarış / 28 yerel API; mobil UI ölçüldü. | 00800/00900 yalnız yerel; üretim/push/deploy yok. |
| 2026-09-09 | Mevcut günlük ihtiyacı düzenleme | Atamaları koruyan 1–100 kişi güncellemesi, eski değer/kapasite/kilit kontrolü. 46 birim/105 DB/27 yarış/24 API geçti; UI hafta 8→9 kişi-gün. | Yedinci migration yalnız yerelde; tam prod kabulü ve native PDF ayrı. |
| 2026-09-09 | Toplu gün talebi ve gerçek CSV indirme | 31 gün önizleme/istisna, atomik kayıt; 45 birim/93 DB/23 yarış/22 API/6 HTTP kontrolü. Tarayıcı 4 talep/8 kişi-gün ve download olayı. | Altıncı migration yalnız yerelde; native PDF kabulü açık, prod/push/deploy yok. |
| 2026-09-09 | Haftalık operasyon ve çıktı | Firma/hafta görünümü, kişi-gün toplamı, günlük bağlantı, güncel veriyle CSV/yazdırma; 39 birim/73 DB/18 yerel API kontrolü. | Beşinci migration yalnız yerelde; native PDF sayfalama ve dosya indirme son konumu kabulü ayrı. |
| 2026-09-09 | Bekleyen işlemleri uzlaştırma | Salt sonuç sorgusu ve onaylı terminal kapatma; 31 birim/62 DB/18 native/15 yerel API kontrolü. UI 1 closed/0 yeni personel. | Dördüncü migration yalnız yerelde; haftalık görünüm/çıktı sırada, prod/push/deploy yok. |
| 2026-09-09 | Pilot kalıcı işlem kurtarma | Hesap/tenant kapsamlı kimlik, Web Locks ve scoped RPC; 28 birim/46 DB/14 yarış/11 yerel API kontrolü, tarayıcı yenileme 1 kayıt/1 olay. Üç migration yalnız yerelde. | Sunucu sonucuyla uzlaştırma üretim öncesi sıradaki kapı; prod/push/deploy yok. |
| 2026-09-09 | Pilot ağ hata akışı | Doğrulanamayan plan boş gösterilmiyor; rol sorgusu hatası ayrıldı, pilot HTTP isteklerine 12 sn sınır eklendi. | 7 kayıp-yanıt servis testi ve yerel tarayıcı kesinti/toparlanma geçti; prod değişmedi. |
| 2026-09-09 | Rol kabulü + mobil kabuk | Operasyon/İK ve açık oturum yetki iptali UI kontrolü geçti. Mobil menü paneli eklendi; 390 px pilot taşması giderildi. | Global kabuk değişikliği; pilot mobil ve masaüstü geçişi doğrulandı, canlıya uygulanmadı. |
| 2026-09-09 | Pilot iptal + CSV tarayıcı kabulü | İptal teyidi uygulama içi dialog; Vazgeç/onay ve CSV tekrar atlama tarayıcıdan doğrulandı. | Yerel temel yönetici akışı geçti; TypeScript/statik başarılı. Üretim değişmedi. |
| 2026-09-09 | Kimlikli tarayıcı kısmi kabul | Canlı giriş ve okuma doğrulandı; yerel sentetik hesapla şube/personel/talep/atama ve kaldırma akışı geçti. | İptal onayı tarayıcı otomasyonunu kilitledi; iptal ve CSV UI kabulü açık. Gerçek parola kaydedilmedi. |
| 2026-09-09 | Yerel Supabase kabulü PASS | Disk engeli çözüldü; PostgreSQL 17.6 + gerçek Auth/PostgREST ve servis koduyla 17 kontrol geçti. | İki migration yalnız sentetik yerel DB'de; prod/deploy değişmedi. Browser ve tarihsel şema/admin kabulü bekliyor. |
| 2026-09-09 | Docker / yerel Supabase kabul hazırlığı | Auth/API test betiği ve ayrı yerel kurulum runbook eklendi. | İlk imaj indirmesi Docker read-only filesystem hatasıyla durdu; host boş alan 549 MiB. Test henüz çalışmadı, canlı değişiklik yok. |
| 2026-09-09 | Native PostgreSQL kabul harness | İki bağımsız backend ve ölçülen kilit beklemesiyle 12 yarış/son durum kontrolü geçti; fixture ortaklaştırıldı, 37 PGlite kontrolü tekrar geçti. | Lokal eşzamanlılık kanıtı; kimlikli Supabase/browser kabulü ve canlı uygulama bekliyor. |
| 2026-09-09 | Toplu şube importu | UTF-8 CSV doğrulama/önizleme, yönetici RPC, firma içi kod tekilliği ve atomik aktarım eklendi. | Lokal; 19 TS testi ve 37 DB kontrolü geçti. İki migration uygulanmadı. |
| 2026-09-09 | Günlük operasyon pilotu + dar SoT güncellemeleri | Altı tablo, iki RPC, server action/service, günlük plan ekranı ve testler eklendi. | Lokal uygulama; migration/deploy yapılmadı, iki bağlantılı yarış ve kimlikli kabul bekliyor. |
| 2026-09-08 | `01_product/ILK_OPERASYON_DILIMI.md`, `src/lib/operations/daily-demand.ts`, `scripts/daily-demand.test.mjs` | Tek günlük talep girdi doğrulaması, doluluk türetme ve atama ön kontrolü yazıldı. Gerçek TS kaynağını çalıştıran 7 test; `qa:operations` komutu eklendi. | İlk dikey dilimin domain temeli. UI/RPC/DB entegrasyonu henüz yok; canlı davranış veya veri değişmedi. |
| 2026-09-08 | `scripts/prepare-test-data-reset.mjs` + test + manual runbook | Yedi tablo/16 satır için offline, varsayılan ROLLBACK SQL üretici yazıldı; tam satır eşitliği, bağımlılık, kayıt sayısı ve korunan tablo kontrolleri. | Kullanıcı tam içerik yedeğini şimdilik reddedip kod hazırlığını istedi. Yedek alınmadı, canlı prova veya silme yapılmadı. |
| 2026-09-08 | Güncel yön + `02_rules/TEST_VERISI_TEMIZLIK_PLANI.md` + Vault handoff/README | Kullanıcı test/örnek iş verisi temizliğini seçti; kod/şema/gerçek hesaplar korunacak. İlk ölçüm 16 iş kaydı ve 2 Storage nesnesi. Vault'a güncel yön yazıldı, eski planlara bağlantı notu eklendi. | Temizlik hazırlığı; yedek/bağımlılık manifesti tamamlanmadan silme yapılmadı. |
| 2026-09-08 | `01_product/BPS_GUNCEL_YON.md` + roadmap/uzlaştırma bağlantıları | Furkan'ın gerçek kullanım olmadığı yönlendirmesiyle plan yeniden çerçevelendi: tek operasyon dilimi → toplu giriş → haftalık pilot. Eski dev projesinin varlığı belirsiz; reset kapsamı cevap bekliyor. | Geçmiş belgeler tarihsel bağlam; sırayı güncel ihtiyaç belirliyor. Veri/kod silinmedi, canlı değişiklik yapılmadı. |
| 2026-09-08 | `01_product/BPS_BASLANGIC_UZLASTIRMA_PLANI.md` + iş planı / MCP ölçüm eki | İlgili Vault handoff ve kararları repo/canlı durumla uzlaştırıldı; P00 devam olarak işaretlendi. bps-dev etiketi düzeltildi; iş hesabı Auth+profiles'ta var, admin bayrağı kapalı ölçüldü. | Kullanıcının önce Vault'u okuyup planlayarak devam etme talebi. Sıra: katalog/dev envanteri → P01 davranış/rol sözleşmesi → P02 lokasyon/import. Kod veya canlı veri değişmedi. |
| 2026-09-08 | `02_rules/SUPABASE_MCP_OLCUM_2026-09-08.md` + teknik plan bağlantısı | BPS prod MCP canlı doğrulaması: postgres/read_only off, PG17.6, 23 RLS tablo, 60 policy, profiles rol UPDATE kapalı. İlk admin sayısı iki sorguda 0; eski açıldı kaydıyla çelişki açık kaydedildi. | Kullanıcının MCP bağlantısı ve kod taraması talebi. Yalnız okuma; yetki/veri değişmedi. Tam şema ve kimlikli davranış denetimi tamamlanmadı. |
| 2026-09-08 | `01_product/BPS_TEKNIK_UYGULAMA_PLANI.md` + iş planı bağlantısı | Supabase veri modeli önerisi, günlük kapasite/çakışma için atomik RPC protokolü, RLS/grant sınırları, idempotent şube importu, kod haritası ve migration/test sırası yazıldı. | Furkan'ın teknik uygulama planı talebi. Repo inceleme noktası `4e3fc00`; yeni canlı ölçüm veya kod/SQL uygulaması yapılmadı. P01'de kesinleştirme, ardından P02 lokasyon/import. |
| 2026-09-08 | `01_product/BPS_OPERASYON_SAAS_IS_PLANI.md` + `TASK_ROADMAP.md` | Ürün görüşmeleri ve SaaS benchmark, P00–P12 iş paketlerine çevrildi; bağımlılıklar, açık kararlar, kabul ölçütleri ve Claude Code uygulama / Codex review akışı yazıldı. Ana yol haritasına bağlantı eklendi. | Furkan'ın Markdown iş planı talebi. İlk adım P00 hazırlığı; kod/SQL değişikliği, migration uygulama, push veya deploy yapılmadı. |
| 2026-09-08 | **PUSH + DEPLOY** — `origin/main` `8f378e0` → `237e06c` (44 commit, 48 dosya, +7110/−263), Vercel `bps-pta7208eo` production **Ready** | Furkan onayıyla push (2026-09-08 01:57 TSİ). Push öncesi üç bağımsız ölçüm: `vercel.json` cron yolu `/api/cron/notifications` route dosyasıyla eşleşiyor · yeni kodun okuduğu env (`CRON_SECRET`, `RESEND_API_KEY`, `BPS_EMAIL_FROM`, `BPS_APP_URL`, `BPS_NOTIFICATION_EMAILS_ENABLED`) — bayrak `"true"` değilse cron **skip** · HEAD tam build ✓. Deploy sonrası kimliksiz canlı kanıt (`www.bpsys.net`; apex `bpsys.net` www'ya 307): `/admin` → 307 `login?returnTo=/admin` (yeni layout) · `/api/cron/notifications` → **401** (yeni route, secret yok) · eski `/api/cron/contract-expiry` → **404** · `/login` 200. Eski deploy'da ilk ikisi 404 dönerdi — canlıdaki kod **yeni kod**. | Vercel CLI cihaz girişi bu oturumda yapıldı (ilk `vercel ls` login akışını tetikledi). **Kimlikli smoke BEKLİYOR (Furkan):** `/firmalar`'da "Yeni Firma" (Mek Group yönetici) · `/admin` girişi (`furkanyahsi@gmail.com`, bayrak) · seçicide yalnız üç mekgroup üyesi + kendi üyesine atama BAŞARIR (verify §7 A/C). Partner kodu temizliği (Step 6) yapılmadı, kod yolları uyuyor. Push'tan sonra yalnız bu docs commit'i lokalde. |
| 2026-09-05 | `supabase/migrations/20260827000400_platform_admin_rpcs.sql` (**UYGULANDI + DOĞRULANDI + repair'lendi**) | **ADMIN PANELİ VERİ KATMANI PROD'DA — üç migration da uygulandı, defter hizalı.** Ön uçuş görülmedi (Claude Chat editöre yükledi, "Success"). Şema kanıtı: `kolon_1=1` (`is_platform_admin` NOT NULL) · `fonksiyon_5=5` (SECURITY DEFINER) · `grant_5=5` (authenticated var, anon yok) · `bayrak_yazilabilir=false` (000200'ün normalizasyonu yeni kolonu da kapsadı — katalog döngüsü çalıştı) · `admin_sayisi=0`. Migration'ın kendi son kontrolleri (gerçek `proowner` için auth USAGE + sessions DELETE + user_id SELECT + RLS bypass + `refresh_tokens(session_id)→sessions(id) CASCADE`) transaction içinde geçti — COMMIT olduğu için kanıtlı. Defter: pending yalnız bilinçli `20260722000200`. | **Kalan adımlar:** (1) ~~ilk platform admin bayrağı~~ **AÇILDI 2026-09-05: `furkanyahsi@gmail.com`** (Furkan'ın raporu "success"; `admin_sayisi` artık 1 olmalı — ilk `/admin` girişi bunu canlıda doğrular). Migration'a gömülmedi (ortam gerçeği), elle açıldı. (2) Panel ve kapsamlı uygulama katmanı **lokal kodda** — canlıya push + deploy ister (Furkan onayı); veritabanı tarafı "migration önce, deploy sonra" şartını sağladı. (3) Sızıntının kapandığının davranış kanıtı: Mek Group oturumuyla seçici testi (verify §7 A/B/C) — hâlâ yapılmadı. Uygulama yolu dersi: pano dört kez bozuldu (mesaj/komut araya girdi) → tam metin sohbete + dosya eki; byte-exact, hash ile. |
| 2026-09-05 | `supabase/migrations/20260904000100_profiles_tenant_scope.sql` (**UYGULANDI + DOĞRULANDI + repair'lendi**), `02_rules/RLS_ACCESS_MATRIX.md`, `supabase/manual/tenant_isolation_test_runbook.md`, `01_product/TASK_ROADMAP.md` | **PROFILES TENANT KAPSAMI PROD'DA.** Ön uçuş ölçüldü: `capraz_atama=0`, `toplam_policy=60`. Uygulama: SQL Editor "Success" (ACCESS EXCLUSIVE + `lock_timeout` altında, kilit hatası olmadı). Şema kanıtı: `toplam_policy=60` **değişmedi** (3 DROP + 3 CREATE net sıfır, çift policy yok) · `profiles` tam 2 policy, SELECT qual `((id = auth.uid()) OR is_active_tenant_member(id))`, `true` yok · `fonksiyon_3=3` (DEFINER+STABLE) · `grant_3=3` (authenticated var, anon yok) · `tasks_guard_2=2`. Defter: `20260904000100` remote'ta; kalan pending `20260827000400` (+ bilinçli `20260722000200`). | **"Migration uygulandı" ≠ "sızıntı kapandı":** kapanış kanıtı gerçek oturumla — Mek Group yöneticisi seçicide yalnız üç mekgroup üyesini görmeli, yabancı üyeye doğrudan PostgREST ataması `42501` ile düşmeli, kendi üyesine atama BAŞARMALI (verify §7 A/B/C; C olmadan A/B anlamsız). Henüz yapılmadı; runbook ve matris buna göre yazıldı. Sıradaki: `000400` (admin paneli) — panoya hazırlanırken **`BEGIN`/`COMMIT`'inin olmadığı** görüldü (diğer ikisinde vardı); editörün implicit transaction'ına dayanmak yerine açıkça sarıldı, yalnız transaction sınırı, mantık aynı; yeni hash `0b4d4853…`. |
| 2026-09-05 | `supabase/migrations/20260904000200_profiles_update_grants.sql` (**UYGULANDI + DOĞRULANDI + repair'lendi**), `02_rules/PROD_SCHEMA_DRIFT.md` | **KENDİ KENDİNE TERFİ AÇIĞI PROD'DA KAPANDI.** Önce: `true/true/true` (canlıydı). Uygulama: SQL Editor "Success". Sonra, ölçüldü: `tablo_update=false · role_yazabilir=false · anon_yazabilir=false · display_name_yazabilir=true`. Defter: `20260904000200` remote'ta; kalan pending `20260904000100` → `20260827000400` (+ bilinçli `20260722000200`). Grantor ölçümü uygulamadan önce alındı: sahip `postgres`, her grant grantor `postgres` → REVOKE'un tutacağı biliniyordu; migration'ın kolon-kolon son kontrolü yine de koştu. | **İki yan bulgu:** (1) `anon`/`authenticated`/`service_role` üçünde de Supabase varsayılanı yedi yetki (TRUNCATE dahil) — yalnız UPDATE normalize edildi, kalanı API'den erişilemez, Faz 2 tüm-tablo kalemi. (2) Panoya yükleme: bu kabukta `LANG`/`LC_ALL` boş, `pbcopy` UTF-8 etiketlemiyor → editöre yapıştırınca Türkçe mojibake; hash eşleşir ama görünüm bozuk. `LC_ALL=en_US.UTF-8 pbcopy` ile çözüldü, memory'ye yazıldı. "Success" bir rapor; kayıt ölçümle kapandı. |
| 2026-09-05 | `02_rules/PROD_SCHEMA_DRIFT.md` (ölçüm kaydı) | **`role_yazabilir` PROD'DA ÖLÇÜLDÜ: `true / true / true` — kendi kendine terfi açığı CANLIYDI.** Dokuz Codex turu boyunca "ölçülmedi, olası" diye taşınan iddia doğrulandı: `authenticated` tablo seviyesinde UPDATE taşıyor, `role` kolonu yazılabilir, `anon`'da bile grant var (RLS bloklar). Faz 0'ın `grant update (display_name)`'i hiçbir şeyi daraltmamış — Supabase varsayılan tablo grant'i altındaydı; `profiles_update_own`'un "Disallow self-promotion" yorumu bir kontrol değildi. `20260904000200` uygulanmak üzere panoya yüklendi (hash `27c086f6…`, uygulama anında yeniden ölçüldü). | Sonuç `display_name_yazabilir=true` beklenen ve migration sonrası da korunacak. Uygulama sonrası beklenen: `false / false / false` + display_name `true`. Açığın **kullanılıp kullanılmadığı** ayrı soru; `profiles.updated_at` ile rol dağılımı karşılaştırılacak. |
| 2026-09-05 | `scripts/qa-static.mjs` (R15 yorumu) | **Codex turu 9 — yeni must-fix YOK; inceleme döngüsü kapandı.** Okuma bütünlüğü geçti: `bad.sql` stat-OK/read-EACCES → R15 FAIL, exit 1; beş sabit `read()` hedefinin her birine ayrı ENOENT enjeksiyonu → R15 FAIL; walker ve sabit liste dışında dosya okuyan başka yol yok. Exit mantığı doğrulandı: yalnız R15 FAIL → 1, yalnız R13 FAIL → 1, PASS/WARN → 0, rapor öncesi yakalanmayan exception → 1. Codex'in R15 için söylediği gerçek güç kuralın kendi yorumuna kelimesiyle yazıldı: **"her listeleme ve her okuma hata vermeden tamamlandı"** — encoding doğruluğu (UTF-16 dosyanın UTF-8 okunması, SQL anahtar kelimesi içinde geçersiz byte) ya da atomik içerik bütünlüğü kanıtlanmıyor ve iddia edilmiyor. | Dokuz turun bilançosu: `490c4f6` → `f5d6129`, 11 commit; SQL beş turdur değişmedi, son dört tur harness'ın kendi dürüstlüğüne gitti. Temiz koşu 16 kontrol · 0 FAIL · 1 WARN · 154 dosya. Prod'da bekleyen ve dokuz turdur değişmeyen iki iş: `role_yazabilir` ölçümü; `000200 → 000100 → 000400` uygulaması. Push HOLD. |
| 2026-09-05 | `scripts/qa-static.mjs` (paylaşılan `read()`, R15) | **Codex turu 8 — okuma bütünlüğü.** Traversal düzeltmesi geçti (FIFO, uzantısız FIFO, FIFO'ya symlink, hard link 154→156, `bad.sql` stat-EACCES, eksik kök hepsi doğru). Kalan tek P2: listelenen dosya `stat`'tan geçip `readFileSync`'te düşerse paylaşılan `read()` `null` döndürüyor, R13 bunu **boş SQL** sayıyor, R15 "listeleme tamdı" diyor — üçü de dar anlamda doğru, dosya denetlenmemiş: **R13 PASS, R14 PASS, R15 PASS, exit 0.** Çözüm merkezi: `read()` `null` sözleşmesini korur ama her okuma hatasını `SCAN_ERRORS`'a yazar (`yol read: KOD`); R15 artık "eksik liste **ya da** okunmamış dosya" der. Yanlış alarm riski ölçüldü: `read()`'in her çağrısı ya sabit-bilinen yol ya walk'tan gelen dosya — "yoksa da olur" bir okuma yok; temiz ağaçta R15 yeşil. Üç vaka, 40 s alarm: `migrations/bad.tmp.sql` chmod 000 (Codex'in enjeksiyonu) → R13 19/19 basar, **R15 `read: EACCES`, exit 1** · `src/lib/unread.tmp.ts` chmod 000 → R14 + R15 kırmızı · temiz → 16 kontrol, 0 FAIL, 1 WARN. | Dedupe doğrulandı: aynı yoldaki farklı hatalar (`stat: EACCES` + `stat: ELOOP`) ayrı ayrı korunuyor — Set metnin tamamını karşılaştırır. Socket/cihaz kodda var, çalıştırılarak test edilmedi. SQL değişmedi; hash'ler aynı. |
| 2026-09-05 | `scripts/qa-static.mjs` (paylaşılan `walk()`, R14, **YENİ R15 `scan-integrity`**) | **Codex turu 7 — iki bulgu; ikincisi tur 6 düzeltmemin yan etkisi.** **(P2 FIFO)** `pipe.ts` adlı bir FIFO `readFileSync`'i sonsuza kadar bloklıyor, koşu sonuç üretemiyordu (Codex 1,5 s dış timeout ile kesti). Kodda yalnız `isDirectory()` ayrımı vardı; dizin olmayan her şey normal dosya sayılıp okunuyordu. Şimdi hem paylaşılan gezgin hem R14'ün katı gezgini yalnız `isFile()` olanı listeye alır; FIFO/socket/cihaz **hiç açılmadan** türüyle raporlanır. **(P2 kısmi liste, R13)** Tur 6'da gezgini "çökmesin" diye stat hatasında girdiyi atlar yapmıştım; Codex `supabase/migrations`'a stat hatalı bir `bad.sql` enjekte etti → R13 **"1/1 PASS"** — eksik kapsam temiz göründü, R14 o dizini taramadığı için telafi etmedi. Çözüm tek yerde: paylaşılan gezgin her readdir/stat hatasını ve her özel dosyayı `SCAN_ERRORS`'a kaydeder; koşunun sonunda **R15 `scan-integrity`** (FAIL ağırlıklı) bu listeyi raporlar — hiçbir kural, harness kırmızıya düşmeden eksik liste üzerinde geçemez. Gezginin üç adımlık tarihi kodda yazılı: fırlatıyordu (raporsuz çökme) → atlıyordu (sessiz eksik kapsam) → atlıyor VE merkezi kaydediyor. Dört vaka, 40 s alarm altında: FIFO `src/lib/pipe.tmp.ts` → R14 + R15 kırmızı, takılma yok · FIFO `supabase/migrations/pipe.tmp.sql` → R15 kırmızı, R13 yine basar · sarkan symlink `migrations/bad.tmp.sql` (Codex'in enjeksiyonu) → R15 kırmızı, R13 N/N basar ama harness kırmızı · temiz → 16 kontrol, 0 FAIL, 1 WARN. **(3 computed)** sabit erişimler geçti; reflection/bind/alias (`Reflect.get`, `const f = c.from`) ve TOCTOU ("tarama atomik snapshot değil") sınır listesine kelimesiyle yazıldı. | Codex'in "önceki kurallar da aynı noktada takılabilir" notu doğruydu ve paylaşılan gezgin düzeltmesiyle kapandı (R13 `read()` FIFO'yu artık hiç görmüyor). Socket davranışı Codex'in sandbox'ında `EPERM` ile test edilemedi; kodda `isSocket()` ayrı türle raporlanıyor, çalıştırılarak doğrulanmadı. SQL değişmedi; hash'ler aynı. |
| 2026-09-05 | `scripts/qa-static.mjs` (R14) | **Codex turu 6 — kısmi dizin taraması ve sabit computed erişim.** **(P2 tarama)** Paylaşılan `walk()` `readdirSync` hatasını yutup kısmi liste döndürüyordu: `src/safe.ts` okunup `src/blocked/` okunamadığında R14 "1 files parsed … PASS" diyor, `blocked/unchecked.ts` ne parse ediliyor ne bildiriliyordu. R14'e özel `walkStrict`: her dizin/stat hatası bir offender, derinlik > 64 → "symlink loop?" FAIL, ELOOP yakalanır, hiçbir şey atlanmaz (`src` altında node_modules/.next yok). Sıfır dosya + tarama hatası varsa hatalar raporlanır. **(P2 computed)** `c["from"]("profiles")` `PropertyAccessExpression` olmadığı için hiç görülmüyordu — dinamik değil, statik çözülebilen aynı çağrı. `memberName()`: `a.b` ve `a["b"]` aynı üye erişimi; sabit-string `ElementAccessExpression` `from`/`eq`/`in`/`select` olarak tanınır, tırmanış onu zincir halkası sayar. Gerçekten dinamik `c[m](...)` belgelenmiş sınır. Yedi vaka, exit koduyla: computed anahtarsız → kırmızı · tamamen computed keyed → yeşil · dinamik metot → geçer (belgeli) · sayfada computed → kırmızı (sayfa yasağı aşılamaz) · chmod 000 alt dizin → kırmızı, kontrollü · `src/lib/.qa-loop.tmp → ..` symlink döngüsü → kırmızı, çökme yok · temiz → yeşil, 154. **Yan bulgu, düzeltildi:** döngü vakası ilk koşuda harness'ı R14'te değil **paylaşılan `walk()`'ta** çökertti — `statSync` try/catch'siz, ELOOP tüm harness'ı raporsuz düşürüyordu (exit 1 ama hiçbir kural satırı yok; tam Codex'in "kontrollü FAIL yerine yakalanmamış hata" notu). Stat hatasında girdi atlanıyor (bu gezgin bilinçli hoşgörülü, metin kuralları için); R14'ün katı gezgini döngüyü FAIL olarak raporluyor; döngü varken 15 kural + özet yine basılıyor. | Codex'in üç bağımsız sayımı (R14 çıktısı · dosya sistemi · `git ls-tree`) 154'te eşleşti — evidence sabit değil, ölçüm. Kalan not doğru: sayı tek başına eksiksizlik garantisi değildi; katı gezginle artık tarama hatası sayıyı sessizce düşüremez. SQL değişmedi; hash'ler aynı. |
| 2026-09-05 | `scripts/qa-static.mjs` (R14) | **Codex turu 5 — R14'ün taramasının kendisi fail-closed; şeffaf sarmalayıcılar.** **(P2)** "Parse etmeden PASS mümkün değil" iddiası yanlıştı: paylaşılan `read()` hatada `null` döndürüyor ve `if (!src) continue` dosyayı **denetlemeden atlıyordu**; `walk()` dizin hatasında boş liste veriyordu — TypeScript yüklüyken **sıfır dosya parse edilerek PASS** üretilebiliyordu; `createSourceFile`'ın parse diagnostics'i de kontrol edilmiyordu (başarısız parse kabul). Şimdi: sıfır `.ts/.tsx` → FAIL ("rule verified nothing", W7 emsali); okunamayan dosya → FAIL (`readFileSync` doğrudan, `EACCES` kodu ile — boş dosya `""` ayrı, parse edilir); `parseDiagnostics` boş değilse → FAIL (ilk hata, satır ve metin); alan yoksa (API değişmiş) → FAIL; PASS satırı **parse edilen dosya sayısını** yazar. Paylaşılan yardımcılara dokunulmadı — metin kuralları için hoşgörü doğru, burada yanlış. **(P3)** `(c.from("profiles")).select("*").eq("id", id)` yanlış alarmdı: `ParenthesizedExpression` tırmanışı durduruyordu. Aynı builder'ı koruyan sarmalayıcılar şeffaf: `(expr)`, `expr!`, `as`, `<T>`, `satisfies`. `await` ve helper argümanı şeffaf DEĞİL (Codex'le mutabık: çalışmış sorgunun sonucunu filtrelemek sorguyu kapsamlamaz; helper'ın davranışı analiz edilmiyor). Dokuz vaka, her biri **exit koduyla** ve "boş çıktı = çökme" kontrolüyle: parantezli keyed → yeşil · parantezli anahtarsız → kırmızı · `as any` + `!` → yeşil · await sonrası filtre → kırmızı · helper argümanı → kırmızı · parse hatalı geçici dosya → kırmızı · chmod 000 dosya → kırmızı · sıfır dosya (kopya) → kırmızı · temiz → yeşil. | Ölçüm dosyasındaki 6. biçim ("boş çıktı geçti değildir") bu turda harness'ın kendisine uygulandı: denetlenmemiş kaynak, denetlenmiş gibi raporlanamaz. SQL değişmedi; Codex `000100` ve `000400` için "mevcut şemaya ilişkin yeni engel yok" dedi (INHERITS sınırı genel, projede kanıtı yok). Hash'ler değişmedi: `000200 27c086f6…` · `000100 dee879c6…` · `000400 2db3940b…`. |
| 2026-09-05 | `scripts/qa-static.mjs` (R14 yeniden yazıldı), `supabase/migrations/20260904000100_profiles_tenant_scope.sql`, `supabase/migrations/20260827000400_platform_admin_rpcs.sql` (ikisi de **UYGULANMADI**) | **Codex turu 4 — SQL kapandı; R14 artık metin değil SÖZDİZİMİ okuyor.** Üç metin tabanlı tarayıcı sırayla yanlış çıktı: satır bölme çok satırlı çağrıyı kaçırdı; `;` dilimi noktalı virgülsüz sonraki cümleye anahtar ödünç verdirdi; parantez-derinlik tarayıcısı yorum/string içindeki `(` ve `;`'yi kod saydı — `/* ( */` sonraki sorgunun filtresini ödünç alabiliyor, `?.eq` ve `/* ; */` meşru zinciri kesiyordu. Dördüncü sürüm regex yaması değil: (b) ve (c) projenin **kendi `typescript` parser'ıyla** gerçek çağrı zincirini izliyor (`from("profiles")` CallExpression'dan ebeveynlere tırmanarak; yorum ve string parser için kod değil, `?.` yalnız bir property access). `typescript` yüklenemezse kural zayıf tarayıcıya **gerilemez, FAIL verir**. Harness başlığındaki "yalnız Node built-in" kuralı bu tek istisnayı açıkça yazıyor. On negatif/pozitif test: ASI ödünç → kırmızı · `/* ( */` ödünç → kırmızı · `?.eq("id")` → yeşil · zincirde `/* ; */` → yeşil · anahtarsız yeni okuyucu → kırmızı · çok satırlı argüman + `.eq` → yeşil · email'de anahtarsız → kırmızı · email'de rol → yeşil · api'de `head: true` → yeşil · `.in("id", allIds)` → geçer (belgelenmiş). **Migration'lar:** `lock_timeout` ifadesi "her kilit beklemesine ayrı, taramayı sınırlamaz" olarak düzeltildi; UNIQUE kontrolüne `indimmediate` (ertelenebilir değil) ve iki kolonda `attnotnull` (NULL'lu çift UNIQUE'i geçer) eklendi; RPC'ye "sıfır satır silinmesi hata değil — kontrol RLS'in sessiz filtrelemeyeceğini kanıtlar, oturumun varlığını değil" notu. | Codex'in kabul edilen sınırları: RLS kontrolü oturum yaşam döngüsünün tamamını kanıtlamaz (oturum hiç olmayabilir, snapshot sonrası açılmış olabilir, projede bulunmayan bir BEFORE DELETE trigger'ı bastırabilir); UNIQUE için "commit edilmiş, NULL olmayan çiftlerde karşı örnek yok". İki migration yeniden hash'lendi (commit mesajında). Sıra: `000200` → `000100` → `000400`. |
| 2026-09-05 | `supabase/migrations/20260904000100_profiles_tenant_scope.sql`, `supabase/migrations/20260827000400_platform_admin_rpcs.sql` (ikisi de **UYGULANMADI**, düzenlendi), `scripts/qa-static.mjs` (R14) | **Codex turu 3 — kilit ve tri-state geçti; üç katalog doğrulaması "vaat ettiği garantiyi vermeyen durumları kabul ediyordu", düzeltildi.** **(1 süre)** "saniyeler" garanti değildi: kilit alımı süresiz bekleyebilir ve `LOCK` iki tabloyu SIRAYLA alır — profiles kilitliyken tasks beklenir. `SET LOCAL lock_timeout = '15s'`: alınamazsa migration temiz düşer, asılı kalmaz; iddia "sınırlı ve düşer" olarak yazıldı. **(2 UNIQUE)** İlk sorgu geçersiz index'i (`indisvalid=false`), ifade sütunlu index'i (indkey'de 0 → pg_attribute'ta satır yok → ad karşılaştırmasından KAYBOLUR; `(tenant_id, user_id, (x+0))` çift ikiliye izin verirken geçerdi) ve INCLUDE sütunlarını ayırt etmiyordu → geçerli+hazır+canlı, kısmi değil, `indexprs IS NULL`, `indnkeyatts = 2`, yalnız anahtar sütunlar (`indkey[0..1]`) karşılaştırılır; PK geçer. **(6 sahip)** `proname` ile arama bir overload'un sahibini seçebilirdi → `to_regprocedure('…(uuid, text, uuid)')`. RLS koşulu çalışma zamanı semantiğiyle birebir: superuser ∨ BYPASSRLS ∨ RLS kapalı ∨ (sahibin yetkilerini HEMEN devralıyor — `USAGE`, `MEMBER` yetmez — ∧ FORCE RLS kapalı); aksi halde DELETE "başarılı" döner ama sıfır satır siler. FK: `refresh_tokens` yoksa ATLANMAZ; tam ilişki doğrulanır — `conkey` → `session_id`, `confkey` → `id`, CASCADE. **(4 R14)** Zincir artık `;`'de bölünmüyor, TARANIYOR (`chainFrom`): parantez açıkken devam, satır sonunda yalnız sonraki satır `.` ile başlıyorsa devam — noktalı virgülsüz sonraki cümle (ASI) anahtarını ödünç veremez, çok satırlı `select(` argümanı zinciri kesmez. Dizin muafiyeti "tamamen bağışık" olmaktan çıktı: `src/lib/email/*` ve `src/app/api/*`'de zincir id/rol ile filtreli ya da `head: true` olmalı. Altı negatif test: ASI → kırmızı · çok satırlı argüman + `.eq("id")` → yeşil · email'de anahtarsız → kırmızı · email'de rol → yeşil · `.in("id", allIds)` → geçer (belgelenmiş, en olası gerçek atlatma) · sayfada çok satırlı → kırmızı. Yanlış-pozitif/yanlış-negatif ayrımı kuralın içinde kelimesi kelimesine. | Codex'in kabul edilen notları: Supabase'in gönderdiği FK gerçekten `refresh_tokens(session_id) → sessions(id) ON DELETE CASCADE`; BYPASSRLS sahibi ACL ve FK kontrollerini vacuous yapmaz; "diğer üyelik yazarları da aynı kilit sözleşmesine uymalı" → RPC yorumuna sözleşme olarak yazıldı (bugün tek yazar RPC + elle SQL). İki migration yeniden hash'lendi (commit mesajında). Sıra: `000200` → `000100` → `000400`. |
| 2026-09-05 | `supabase/migrations/20260904000100_profiles_tenant_scope.sql` (**UYGULANMADI**, düzenlendi), `src/lib/supabase/profiles.ts`, `scripts/qa-static.mjs` (R14) | **PROFILES TENANT KAPSAMI — Codex turu 2 (856a219'a karşı): A ve C geçti, B ve D düzeltildi.** **(B kilit)** SHARE ROW EXCLUSIVE + "okuyucuları engellemez" iki yerden yanlıştı: DROP/CREATE POLICY zaten ACCESS EXCLUSIVE alıp transaction sonuna kadar tutar (okuyucular DDL boyunca zaten bekler) ve zayıftan güçlüye YÜKSELTME döngü kurar — tasks'ı okumuş bir transaction (ACCESS SHARE) yazmak isteyip bizim arkamızda bekler, biz DDL için onun bitmesini bekleriz → deadlock. Düzeltme: en güçlü kilit BAŞTAN, `profiles` ve `tasks`'a birden, tek `LOCK TABLE … ACCESS EXCLUSIVE` — yükseltme yok, kendi döngümüz yok; bedeli yazılı: iki tablo transaction boyunca (saniyeler) okunamaz. Tablolar-arası deadlock genel olarak dışlanamaz; dedektör bir tarafı düşürürse bizimki temiz geri alınır. **(A eksik kanıt)** `UNIQUE(user_id, tenant_id)` repo'da yalnız yorumdu → ön kontrol artık pg_index'ten ölçüyor (tam bu iki kolon, kısmi olmayan unique); hook'un `v_count = 1`'i ve admin RPC'nin küme hesabı buna dayanır. **(C sınır)** `{data: null, error: null}` "üye değil" sayılıyordu → boolean olmayan cevap "doğrulanamadı" (throw). **(D R14)** `from ("profiles")` boşluğu yakalanıyor (`\.from\s*\(`); **yeni dal (c):** src altındaki her ham profiles zinciri `;`'ye kadar `.eq("id"`/`.in("id"` taşımak zorunda — Codex'in asıl örneği ("farklı adla `src/lib`'de okuyucu") bununla kırmızı; izinli: `src/lib/email/*` (service_role, rol/id filtreli, `loadTenantScope`) ve `src/app/api/*` (healthz). Dört negatif test: boşluklu → kırmızı, anahtarsız yeni okuyucu → kırmızı, id-anahtarlı yeni okuyucu → yeşil, çok satırlı → kırmızı (regresyon). Kalan sınırlar kuralda yazılı: dinamik tablo adı, takma ad, cümleler arası kurulan zincir, "anahtarlı görünen" `.in("id", herkes)`. | Çok satırlı atlatma zaten `95a72a6`'da kapanmıştı (verdict ondan önceki commit'e karşı). Codex'in "canlı üyelik = snapshot'ta görünen üyelik; koşan cümle geriye dönük iptal edilmez" notu doğru ve kabul — snapshot semantiği, hata değil. `000100` yeniden hash'lendi (commit mesajında); sıra değişmedi: `000200` → `000100` → `000400`. |
| 2026-09-05 | `supabase/migrations/20260827000400_platform_admin_rpcs.sql` (**UYGULANMADI**, düzenlendi), `scripts/qa-static.mjs` (R14, W7 etiketi) | **ADMIN PANELİ — Codex turu 2, iki bulgu kapatıldı; ikisi de tur 1'de benim eklediğim kodda.** **(P1 yarış)** Oturum iptali kararı (`v_membership_changes`) profile satır kilidinden ÖNCE hesaplanıyordu. Aynı kullanıcıya iki eşzamanlı çağrı aynı başlangıç kümesini okur: A→B "değişiyor", A→A "değişmiyor"; ilki commit eder, ikincisi kilidi bekleyip B→A yazar ama önceden hesapladığı `false` yüzünden oturumları silmez — B için açılmış oturum yaşar. Düzeltme: yetki kapısından hemen sonra `PERFORM 1 FROM profiles WHERE id = p_user_id FOR UPDATE` (varlık kontrolü de aynı satır); hesap ve yazmalar kilit altında; ikinci çağrı READ COMMITTED taze snapshot'la committed kümeyi görür. **(P2 yanlış ölçüm)** `has_table_privilege('auth.sessions','DELETE')` yalnız `current_user`'ı ölçüyordu — ama `CREATE OR REPLACE` mevcut fonksiyonun SAHİBİNİ değiştirmez, fonksiyon `proowner` olarak koşar; ve `DELETE … WHERE user_id` ayrıca auth şemasında USAGE + `user_id` SELECT ister. Ön kontrol vaat ettiği garantiyi vermiyordu (REVIEW_STANDARD §9'un tarifi). Düzeltme: fonksiyon YARATILDIKTAN sonra, katalogdan okunan gerçek sahip için USAGE + DELETE + SELECT(user_id) + (RLS varsa bypass/üyelik) + **`refresh_tokens → sessions` FK'sinin ON DELETE CASCADE olduğu** doğrulanır — CASCADE değilse DELETE FK ihlaliyle düşer ve her tenant değişikliği panelden yapılamaz olurdu. Kalan pencere ifadesi düzeltildi: "tam 3600 s" değil, **token'ın kalan geçerlilik süresi**. **R14** çok satırlı `.from(\n"profiles"\n)` atlatmasını yakalıyor (tüm kaynak üzerinde, `\s*` satır sonlarını kapsar); dört durum negatif test edildi. `unreachable-component` yorumunun kendini "R14" diye etiketlemesi (başlıkta W7) benim R14 ile çakışıyordu → W7. | **Kayda değer, düzeltilmedi:** `updateOwnDisplayName` servisinin hiçbir çağıranı yok — `display_name` grant'i bugün hiçbir ekranın kullanmadığı bir yolu koruyor (Faz 0 niyeti; korundu, silinmedi). Codex'in "grant döngüsü çalıştığı andaki kolonları kapsar, gelecekteki GRANT'i sürekli denetlemez" notu doğru — verify dosyası yeniden koşulabilir, sürekli denetim değil. Grant açığının BUGÜN canlı olup olmadığı hâlâ ölçülmedi (`role_yazabilir` sorgusu). `000400` yeniden hash'lendi (commit mesajında). |
| 2026-09-05 | `supabase/migrations/20260904000100_profiles_tenant_scope.sql` (**UYGULANMADI**, düzenlendi), `supabase/migrations/20260827000400_platform_admin_rpcs.sql` (**UYGULANMADI**, yorum), `supabase/manual/profiles_tenant_scope_post_apply_verify.sql`, `src/lib/supabase/profiles.ts`, `src/lib/services/{tasks,platform-admin}.ts`, `scripts/qa-static.mjs` (R14), `02_rules/RLS_ACCESS_MATRIX.md` | **PROFILES TENANT KAPSAMI — Codex turu 1, üç bulgu kapatıldı.** **(P1 claim)** İki yardımcı da `current_user_active_tenant()`'ın JWT claim'ine güveniyordu: A'dan B'ye taşınmış kullanıcının eski A token'ı `active_tenant_profiles()`'tan A'nın profillerini almaya devam ederdi. Bu, dün "gövdesi elimizde yok" diye ertelenen kararın **ucuz hâli**: `current_user_active_tenant()`'a dokunmadan, yeni yardımcı `current_user_verified_tenant()` claim'i yalnız çağıranın o tenant'ta HÂLÂ üyeliği varsa döner (yoksa NULL); iki fonksiyon tenant'ı ondan alır. Eski claim ne eski ne yeni tenant'a erişim verir — fail-closed. **Claim ipucu, üyelik tablosu gerçek.** Kalan pencere yalnız claim'e güvenen 43 policy için (oturum iptaliyle JWT süresine sınırlı). **(P1 yarış)** Ön kontrol (e) ile policy DDL'inin kilidi arasında eski policy altında çapraz-tenant görev yazılabilir, migration commit eder, satır kilitli kalırdı → BEGIN'den hemen sonra `LOCK TABLE tasks IN SHARE ROW EXCLUSIVE MODE` (yazarların ROW EXCLUSIVE'iyle çatışır, transaction sonuna kadar, okuyucuları engellemez). **(P2)** `isActiveTenantMember` RPC hatasını `false` ile birleştiriyordu → aynı tenant üyesine geçici hatada "üyesi değil" derdi. Artık fırlatır; `resolveAssignee` üç durumu ayırır: üye / üye değil / **doğrulanamadı** — yazma her ikisinde durur, mesaj farklı. **R14** tek/çift tırnak + boşluk yakalar, bilinen sınırları yorumda (dinamik tablo adı, `.ts` yardımcı) — tripwire, kanıt değil. | Codex'in geçen kontrolleri kayda değer: üç CASE bloğu 20260827000300 ile **byte-identical** (SHA-256 ilk12 `c5212d355767`); doğrudan EXISTS'in çalışmayacağı PostgreSQL dokümanıyla teyit; temp tablo + `ON COMMIT DROP` SQL editor'da geçerli. Migration hash'i değişti: **`000100` yeniden hash'lendi** (commit mesajında). Uygulama sırası değişmedi: `000200` → `000100` → `000400`. |
| 2026-09-04 | `supabase/migrations/20260904000200_profiles_update_grants.sql` (YENİ, **UYGULANMADI**), `supabase/migrations/20260827000400_platform_admin_rpcs.sql` (**UYGULANMADI**, düzenlendi), `src/app/admin/{page,AdminClient}.tsx`, `src/lib/services/platform-admin.ts`, `02_rules/PROD_SCHEMA_DRIFT.md` | **ADMIN PANELİ — Codex turu 1, üç bulgu kapatıldı.** **(P1 grant)** `is_platform_admin`'in yazılamazlığı migration'da yorumla varsayılıyordu. Ölçüm: `profiles_update_own` WITH CHECK yalnız `auth.uid() = id`; "Disallow self-promotion" **yorum, kontrol değil**. Rolü koruyan tek şey Faz 0'ın kolon grant'i — ve Faz 0 tablo seviyesi UPDATE'i REVOKE etmedi; Supabase varsayılanı tablo seviyesinde ALL verir. Tablo grant'i prod'da duruyorsa **her kullanıcı bugün kendi rolünü `yonetici` yapabilir** — bayraktan bağımsız ve ondan ağır; ölçülmedi, sorgu drift dokümanında. Yeni migration `000200` grant'i normalize eder (tablo seviyesi kalkar, `display_name` kalır) ve **katalogdaki her kolonu fail-closed** doğrular; `000400` artık uygulanmadan önce bunu ŞART koşar. **(P1 oturum)** Rol canlı okunur ama tenant JWT claim'i: üyelik değişince eski token eski tenant'ı taşıyor, refresh'te yenileniyordu — rol+üyelik atomik, oturum yarım. RPC üyelik kümesi değiştiğinde `auth.sessions`'ı siler: refresh imkânsızlaşır, kullanıcı en geç JWT süresi dolunca yeniden girer ve doğru claim'i alır (sıfır üyelikten bire geçen Mek Group kullanıcıları için de gerekli — aksi halde düzeltme yapılmış görünür, ekran token yenilenene kadar boş kalır). Yalnız rol düzeltmesinde oturum korunur. `auth.sessions` varlığı ve DELETE yetkisi migration anında doğrulanır, ilk kullanımda değil. **(P2)** `page.tsx` ham `Error.message` gösteriyordu → yalnız `PlatformAdminError`. | **Kalan pencere açıkça kaydedildi:** mevcut access token süresi (varsayılan 3600 s) boyunca eski token eski tenant'ı okur. Tam kapatmak `current_user_active_tenant()`'ın claim'i canlı üyelikle doğrulamasını ister; o fonksiyon repo dışında ve gövdesi elimizde yok (`TASK_ROADMAP`: "tahminle tamamlanamaz") — **ayrı karar, gövde okunmadan yazılmaz.** JWT süresi Auth ayarından kısaltılabilir. **Uygulama sırası: `000200` → `20260904000100` → `000400`.** |
| 2026-09-04 | `supabase/migrations/20260904000100_profiles_tenant_scope.sql` (YENİ, **UYGULANMADI**), `supabase/manual/profiles_tenant_scope_post_apply_verify.sql` (YENİ), `src/lib/supabase/profiles.ts`, `src/lib/services/{profiles,tasks,platform-admin}.ts`, `src/app/(main)/{gorevler,randevular,ayarlar}/page.tsx`, `src/types/database.types.ts`, `scripts/qa-static.mjs` (R14), `02_rules/RLS_ACCESS_MATRIX.md`, `supabase/manual/{README,tenant_isolation_test_runbook}.md`, `01_product/TASK_ROADMAP.md` | **ÇAPRAZ-TENANT SIZINTISI — canlı, iki yönlü, ölçüldü.** Mek Group kullanıcısı görev seçicisinde Partner Staff kullanıcılarını görüyordu (okuma); yöneticisi onlara görev atayabiliyordu (yazma — `tasks_insert/update` WITH CHECK yalnız görevin tenant'ını denetliyor, atananın değil). Sebep: `profiles`'ta `tenant_id` yok, `profiles_select_authenticated` `using (true)`, uygulama katmanında kapsamsız `select *`. **Sürpriz değil:** `20260722000100` riski G1/G3 insan gate'iyle ertelemişti; tenant 1 → 4 olunca gate'in öncülü sessizce geçersizleşti — **beyan sorgu değildir.** **ÜÇ KATMAN, biri Furkan'ın önerisinden AYRILARAK:** (1) RLS — `tenant_id` kolonu EKLENMEDİ (tekil kolon, ileride çok-üyelik); kapsam `tenant_memberships` üzerinden ama **doğrudan `EXISTS` DEĞİL**: policy ifadesi çağıranın yetkisiyle koşar ve `tenant_memberships`'te `authenticated` grant'i yok — önerilen SQL uygulansaydı her profiles okuması ve her görev insert'i `permission denied` ile düşer, uygulama açılmazdı. Doğru desen kod tabanında zaten vardı: `current_user_has_company_scope()` tam bu yüzden `SECURITY DEFINER`'dı. Yeni yardımcı `is_active_tenant_member(uuid)`; profiles SELECT → `id = auth.uid() OR is_active_tenant_member(id)` (kendi satırı koşulsuz — sıfır üyelikli kullanıcı "boş ekran"dan "giremiyor"a düşmesin, admin panelinin teşhis edeceği durum panelden önce kilitlenmesin). (2) YAZMA — aynı fonksiyon `tasks_insert` VE `tasks_update` WITH CHECK'inde (`assigned_to_user_id IS NULL OR …`); yalnız insert'e koymak açığı kapatmazdı. CASE blokları 20260827000300'den birebir, sadeleştirilmedi. (3) UYGULAMA — `active_tenant_profiles()` RPC (parametresiz, tenant sunucuda çözülür); kapsamsız `selectAllProfiles` SİLİNDİ, `listProfiles` → `listActiveTenantProfiles` (kapsam her çağrı yerinde görünür); `ayarlar` sekmesinin servis katmanını atlayan ham sorgusu da aynı sızıntının ikinci çağrı yeriydi, servise bağlandı; `resolveAssignee` üyeliği RLS'in çağırdığı AYNI fonksiyonla önceden denetler ve Türkçe mesaj verir. **`qa:static` R14** (FAIL): kapsamsız okuyucu sembolü ya da sayfada ham profiles sorgusu geri gelirse kırmızı; iki dal da negatif test edildi. | **Migration kendi kendini doğruluyor — sessiz yarım durum imkânsız:** ön kontrol policy ADLARINI doğrular (`contracts` dersi: ad yanlışsa DROP no-op, CREATE ikinci permissive policy ekler, OR ile birleşir, sızıntı sürer, migration "başarılı" der), mevcut çapraz-tenant atama varsa DURUR (yeni WITH CHECK o görevlerin her güncellemesini reddederdi — karar elle: verify §6); son kontrol toplam policy sayısının DEĞİŞMEDİĞİNİ ve profiles'ta `true` kalmadığını doğrular, tutmazsa COMMIT olmaz. **Bilinen sonuç:** admin paneli bir kullanıcıyı başka tenant'a taşırsa eski tenant'taki görevleri yeniden atanana kadar güncellenemez — sessiz değil, `platform-admin.ts`'te ve verify §6'da yazılı. G1/G3 marker'larına DOKUNULMADI (R12; o gün doğruydu), altına geçersizlik notu düştü. **Sıra: admin panelinden ÖNCE uygulanır** — panel aynı profiles listesini kullanacaktı. tsc 0 · build ✓ · qa:static 15 · 0 FAIL · 1 WARN · qa:unit PASS. |
| 2026-08-27 | `supabase/migrations/20260827000400_platform_admin_rpcs.sql` (YENİ, **UYGULANMADI**), `src/app/admin/{layout,page,AdminClient,actions}.tsx|ts` (YENİ), `src/lib/services/platform-admin.ts` (YENİ), `src/types/database.types.ts` | **PLATFORM ADMIN PANELİ — FAZ 1.** Gerekçe ölçülmüş bir hata: Mek Group tenant'ı kurulurken rol+üyelik ataması ÜÇ KEZ elle yapıldı, üçünde de bir parçası atlandı (rol atanmadı · hesap silinip açılınca ikisi birden gitti · rol düzeltildi ama üyelik insert'i koşmadı). **Hiçbiri hata vermedi** — kullanıcı giriş yaptı, boş ekran gördü, sebep ancak DB'den anlaşıldı. `updated_at` kolonu + trigger dersinin aynısı: yarısını yapmak, yapmış görünüp yapmamak. **ÜÇ KARAR:** (1) **`is_platform_admin` bir BAYRAK, rol DEĞİL** — roller göreve göre tasarlanır; ayrıca prod'da 4 tenant ve birden fazla `yonetici` var (Mek Group'ta 3), yani `role='yonetici'` platform admin'i AYIRT ETMEZ. Bayrak `profiles.role` CHECK'ine ve 60 policy'ye dokunmuyor, Step 3'ü büyütmüyor. (2) **Her şey `SECURITY DEFINER` RPC üzerinden, `service_role` YOK** — `tenants`/`tenant_memberships` PostgREST'e kapalı (RLS açık, policy 0, grant yok) ve bu KASITLI; policy EKLENMEDİ. RPC yolu `qa:static`'in `service_role-confined` kuralını da bozmuyor (doğrulandı: 0 FAIL). Yetki kapısı RPC'nin İÇİNDE (`42501`), UI kapısı ikinci katman. (3) **Üyelik EKLENMİYOR, DEĞİŞTİRİLİYOR** — `custom_access_token_hook` yalnız tek üyelikte claim yazıyor; ikinci üyelik kullanıcının erişimini SESSİZCE sıfırlardı. RPC önce tüm üyelikleri siler, sonra tekini yazar: çift üyelik panel üzerinden YAPISAL OLARAK imkânsız. | **ATOMİKLİK RPC GÖVDESİNDEN GELİYOR** — `supabase-js` transaction desteklemiyor, iki ayrı çağrı + telafi tam da bugün "telafi sessizce başarısız olur" diye öğrendiğimiz şey olurdu. Fonksiyon gövdesi tek transaction: rol yazılıp üyelik yazılmadan çıkmak MÜMKÜN DEĞİL. **Sayfa ağacı ŞİMDİ ayrıldı** (`/admin`, kendi layout'u) — sonradan ayırmak "admin'i klonla, birkaç izin kaldır" desenine dönüşür; Furkan bugün hem platform admin hem üç tenant'ın yöneticisi, tek panel ikinci kullanıcı grubunda ayrıştırılamazdı. **Panelin asıl işi sessiz yarım durumu GÖRÜNÜR kılmak:** `uyelik_sayisi` sütunu 1 dışında her değerde kırmızı uyarı gösteriyor (0 → claim yazılamaz, 2+ → hook takılır; ikisinde de kullanıcı giriş yapar ve hiçbir şey göremez). Rol ve kiracı TEK FORM, ayrı kaydedilemiyor. **FAZ 2 (tenant admin, `/ayarlar`) kapsam dışı** — ikinci gerçek kullanıcı grubu olunca. **İlk platform admin bayrağı migration'a GÖMÜLMEDİ** (e-posta ortam gerçeği, şema değil; gömülen hesap başka ortamda yanlış kişiye yetki verir) — uygulama sonrası elle açılacak. |
| 2026-08-27 | `supabase/migrations/20260827000300_remove_partner_role.sql` (YENİ, **UYGULANDI**), `supabase/manual/partner_removal_runbook.md` (YENİ), `supabase/manual/tenant_isolation_test_runbook.md` (YENİ), `03_strategy/BPS_DURUM_RAPORU_VE_YOL_HARITASI.md` (YENİ) | **PARTNER ROLÜ KALDIRILDI — RLS katmanı.** Karar Furkan'ın: partner kalkıyor, kesin. `satis@bps.local` silindi (rol değiştirilmedi), 30 policy tek transaction'da yeniden yazıldı, defter 33 → 34. **Prod'daki 60 policy'nin 31'i partner'a dokunuyordu**; biri `partner_company_assignments`'ta ve ADIM 5'te tabloyla düşecek, 30'u düzeltildi. **Tek şablon YAZILAMADI — 5 desen çıktı:** rol listesi(2) · CASE-rol(20) · OR bloğu(2) · CASE+EXISTS(4: `contacts`'ta `tenant_id` kolonu YOK, tenant `companies` üzerinden türetiliyor, EXISTS aynen korundu) · **CASE-VERİ(2: `documents` `contract_id IS NULL` üzerinden dallanıyor, partner İKİ daldan silindi — tek daldan silmek policy'yi çalışır bırakır ve partner bir veri koşulunda hâlâ erişirdi, SESSİZ hata)**. Dördüncü desen 12., beşinci 21. policy'de çıktı: hiçbir noktada "yeter" demek savunulabilir değildi. **6 sadeleşme** (iki dallı CASE'ler düz ifadeye indi; `financial_summaries`'te `company_id IS NOT NULL` guard'ı da gitti — yalnız partner bacağını koruyordu). **6 incelik tek tek uygulandı:** dal sayısı · CASE sarmalı (`notes_insert`'te dış `author_id` guard'ı KORUNDU — düşürmek herkesin başkası adına not yazmasına yol açardı) · 7 UPDATE'te QUAL+WITH_CHECK ikisi de · EXISTS vs düz · CASE koşulu veri olabilir · `workforce_summary`'de dal asimetrisi (`ik` select'te var, yazmada yok — korundu). | **SON ANDA YAKALANAN SESSİZ HATA:** `contracts` üçünün policy adı `_role_or_scope` eksik yazılmıştı (ham metinler kısaltılarak aktarılmıştı). Uygulansaydı `DROP` no-op olur, `CREATE` İKİNCİ bir policy yaratır, eski partner'lı policy YERİNDE KALIR — ve **permissive policy'ler OR ile birleştiği için partner sözleşmelere erişmeye DEVAM ederdi**, migration ise "başarılı" dönerdi. Prod ad listesiyle diff'lenerek yakalandı. **Ders: gövdeyi md5'lemek yetmez, ADI da doğrula** — 30 gövde parmak iziyle doğrulanmıştı ama adlar değil. **Doğrulama 6/6:** `auth_kalan=0 · profil_kalan=0 · partner_rolu=0 · partner_policy=1 (yalnız pca) · contracts_policy=4 (3 düzeltildi + `contracts_delete_admin`, partner içermiyordu) · **`toplam_policy=60` DEĞİŞMEDİ**` ← 30 DROP + 30 CREATE net sıfır, çift policy yok; ad hatası tekrarlansaydı 63 olurdu. Geri dönüş planı runbook'ta: 30 policy'nin ÖNCEKİ ham metni. **Kalan:** izolasyon testi → CHECK + tablo/fonksiyon DROP → 43 dosyalık kod temizliği. |
| 2026-08-27 | `02_rules/RLS_ACCESS_MATRIX.md` (YENİ), `02_rules/PROD_SCHEMA_DRIFT.md` (YENİ), `02_rules/REVIEW_STANDARD.md` (§9 YENİ), `00_core/CODEX.md` | **Prod ölçümleri repo'ya taşındı — repo-kısıtlı denetleyicinin (Codex) göremediği gerçek.** Codex erişimi repo köküyle sınırlı; Vault okunamıyor, ama asıl boşluk vault↔repo değil **prod↔repo**. Üç somut örnek bu dosyalar olmadan yanlış sonuca götürüyor: `companies_insert_yonetici` (repo yorumu "policy gelmedi" diyor, prod'da VAR) · `tenant_id NOT NULL` + DEFAULT yok (bilinmezse "IF NOT EXISTS yeterli" denir — bu hata bugün bir kez yapıldı) · `financial_summaries` NOT NULL kısıtları. **(1) `RLS_ACCESS_MATRIX.md`** — 19 tablonun SELECT rol kapsamı, tam sayım. **(2) `PROD_SCHEMA_DRIFT.md`** — Faz 1 envanteri (4 tablo · 2 fonksiyon · 12 kolon · 43 policy repo dışı) + `md5` drift guard (`custom_access_token_hook` ilk12 `758af650dcaa`) + `migration repair` sınırları. **(3) `REVIEW_STANDARD` §9** — beş ölçüm hatası biçimi + altıncısı ajanlara özel (aracı olmayan ajan değer uydurur; bir sha256 tam böyle uydurulup apply adımına girdi, ikinci ajan gerçeğini hesapladığı için yakalandı) + karşı önlemler. **(4) `CODEX.md`** — iki dosya "her zaman oku" listesine girdi. | **ÇAPRAZ DOĞRULAMA YENİ BİR BULGU ÜRETTİ: `db push` yasağının İKİNCİ gerekçesi var.** Bilinen gerekçe tenant izolasyonuydu; ölçüldü ki **rol izolasyonu da aynı yoldan bozuluyor** — repo'nun `tasks_select`'i `goruntuleyici`'yi İÇERİYOR (20260407000800:55), prod'unki içermiyor; `documents_select` için `muhasebe`+`goruntuleyici` aynı durumda (20260407001000:58). `db push` sonrası bu roller görev ve evrak okumaya başlar — **ROLE_MATRIX §4'ü ihlal ederek**. Yani prod DOĞRU, repo ESKİ; Faz 2'nin işi repo'yu prod'a hizalamak. **Kapatılmayan bir ölçüm belirsizliği açıkça işaretlendi:** kaynak ölçüm bir grubu "5 rol, muhasebe/görüntüleyici hariç" diye kaydetti ama altı rolden ikisi çıkınca dört kalır; iki okuma da mümkün ve fark `goruntuleyici`'nin görev/evrak okuyup okumadığı demek. **Tahmin edilmedi**, kapatan sorgu dosyaya yazıldı. **Codex'in kalıcı uyarısı belgelendi:** Faz 2 prompt bağımlılığını azaltır ama SIFIRLAMAZ — repo'ya alınan şey o günkü ölçümün FOTOĞRAFIDIR. Bugün prod'un repo'dan saptığı üç kez görüldü, bu yüzden `db push` yasağı ve md5 guard Faz 2'den sonra da kalır. Her iki dosya da ölçüm tarihini taşır ve "kullanmadan önce sor: bu ölçüm ne zaman alındı, o tarihten sonra prod'a DDL gitti mi?" kuralını dayatır. Faz 1 envanterini taşırken 17 günde üç sayının değiştiği görüldü (tablo 21→23, trigger 14→16) — dosyanın kendi tezi kanıtlandı. |
| 2026-08-27 | `supabase/migrations/20260827000200_create_notification_log.sql` (YENİ), `src/lib/notification-kinds.ts` (YENİ), `src/lib/email/{notification-log,notification-recipients,notification-batches}.ts` (YENİ), `src/app/api/cron/notifications/route.ts` (YENİ), `src/app/api/cron/contract-expiry/route.ts` (SİLİNDİ), `src/lib/email/contract-expiry-email.ts`, `vercel.json`, `src/types/database.types.ts` | **Batch 10'un "notification/push/badge introduced değil" kararı KISMEN geri alındı (Furkan onayı): yalnız E-POSTA açıldı.** Push, badge ve uygulama içi bildirim merkezi AÇILMADI — A aşamasının ayrı kararı; üç closeout kaydına bu ayrımla kapsam şerhi düşüldü. Dört tip: `contract_expiry` (mevcut, 30g) · `task_overdue` · `document_expiry` (30g) · `appointment_reminder` (1g). **Dört tasarım kararı, hepsi ölçüme dayalı ve migration'da gerekçeli:** (1) tip başına tablo DEĞİL, tek `notification_log` — plan 6+ tip sayıyor, tip başına tablo 6 migration+6 RLS+6 servis demekti; bedeli polimorfik `entity_id` (FK yok, cascade yok) ve ödenebilir çünkü defter append-only: yetim satır dört tipte de mümkün ama zararsız — **ilk gerekçe ("`contracts`/`tasks`'ta DELETE policy yok") YANLIŞTI ve düzeltildi**; `grep "FOR DELETE"` case-sensitive'di, tam sayım 12 tabloda DELETE policy gösteriyor (append-only defter, silinen kayıt yeniden yaratılırsa yeni id → yanlış "gönderildi" ÜRETİLEMEZ). İdempotency PK `(kind, entity_id, recipient_profile_id, threshold_key)` — mevcut üçlünün genellemesi; `threshold_days integer` yerine `threshold_key text` çünkü "overdue" bir gün sayısı değil. (2) `tenant_id` AÇIK ve NOT NULL, dolaylı türetme değil — polimorfik hedefte tek join yolu yok, ileride bir RLS yazılırsa dolaylı tenant onu hedef tabloya bağımlı kılardı, ve bugün kapatılan tenant borcunu geri açmamak için. Değer VERİDEN (tetikleyen kaydın kendi tenant'ı), oturumdan DEĞİL. (3) RLS açık, **policy SIFIR** — mevcut `contract_expiry_emails_sent` deseni; defter sistem kaydı, kullanıcı yüzeyi yok. "Gönderilen bildirimler" ekranı istenirse o zaman rol+tenant kapılı SELECT policy'si eklenir. (4) Eşikler KODDA sabit — ölçüldü: `Ayarlar > Bildirim Kuralları` sekmesi MEVCUT ama BOŞ placeholder (`ayarlar/page.tsx:617`), bağlı kural altyapısı yok; konfigürasyon B'nin kapsamı değil. `kind` üzerinde CHECK VAR (yeni tip = migration = açık onay), `threshold_key` üzerinde YOK. | **SPAM KORUMASI: alıcı başına TİP başına TEK mail** — on geciken görev on mail değil, tek mailde on satır; damgalar kalem başına atıldığı için ertesi gün yalnız YENİ kalemler anlatılır. Bu bir "günlük özet" DEĞİL: digest tipleri tek mailde birleştirir ve Contract Expiry canlı pilotuna sıralama-bloklu kalır. **Gönderim sırası mevcut akıştan devralındı:** stamp-first → send → başarısızsa stamp rollback; rollback'in kendisi başarısız olursa SESSİZ GEÇİLMEZ (o kalem kalıcı "gönderildi" görünür ve mail hiç gitmez — loga `ROLLBACK FAILED` düşer). **Alıcı kuralı: bildirim yetki genişletmez** — `document_expiry` yonetici+ik (ROLE_MATRIX §4 evrak okumasıyla hizalı; `operasyon` bilerek dışarıda, gürültü), `task_overdue` görevin sahibine (`assigned_to_user_id`, A batch'i açtı) ve sahipsizse yöneticiye (sahipsiz iş sessizce kaybolmaz), `appointment_reminder` firma tarafına — çünkü **ölçüldü: `appointments`'ta sorumlu kolonu YOK**. **Ölçümün yakaladığı iki varsayım:** `appointments.subject` diye bir kolon yok (gerçekte `meeting_type`+`attendee`) ve env adları `BPS_EMAIL_FROM`/`BPS_APP_URL` (uydurulan adlar ops'ta tanımlı değişkenleri sessizce devre dışı bırakırdı). `tasks.due_date` bir `text` kolonu olduğu için gecikme karşılaştırması ISO süzgecinden geçiriliyor — siper, çözüm değil; kolonun `date` olması ayrı kalem. **TENANT KAPSAMI (Codex 2. tur, P1):** `profiles`'ta `tenant_id` OLMADIĞI için "role göre profil çek" sorgusu kaçınılmaz olarak BÜTÜN tenant'ları getiriyordu ve cron `service_role` ile koştuğu için RLS de daraltmıyordu → her `yonetici`/`ik` BAŞKA tenant'ların evrak/görev/randevu bildirimlerini alırdı, kayıt adlarıyla. Üç yolun üçü de açıktı (rol · owner · firma). Düzeltme: `tenant_memberships` (repo dışı tablo, read-only tiplendi) üzerinden **fail-closed** tenant kapsamı — harita yüklenemezse HİÇ mail gitmez, sızdırmaktansa bir koşu kaçmak. Filtre tek yerde (gruplu gönderim) uygulanıyor ve düşen kalem sayısı cron logunda `cross_tenant_dropped` olarak raporlanıyor. **CUTOVER SIRASI KİLİTLENDİ (Codex P1):** naif "migration → deploy" sırası bir pencere bırakıyordu — migration sonrası, deploy öncesi eski cron çalışırsa yeni damgayı ESKİ tabloya yazar, backfill fotoğrafı çekilmiş olduğu için o damga taşınmaz ve ileride duplicate mail üretir. Doğru sıra `TASK_ROADMAP`'e yazıldı: eski flag kapat → **skip'i DOĞRULA** → migration → `count(*)` karşılaştır (N ≥ E, değilse deploy YOK) → deploy (yeni flag kapalı) → skip doğrula → flag aç. **PARTNER BİLDİRİMLERİN DIŞINDA (Codex 2. tur, kalan P1):** `partner` hem `task_overdue`'dan hem `appointment_reminder`'dan çıkarıldı. Tenant filtresi tenant'ı doğruluyor ama `partner_company_assignments`'ı DEĞİL; picker kapsamsız olduğu için portföy dışı atama mümkün; ve **FROZEN/HOLD olmak e-posta teslimatını durdurmaz** — cron o adrese yine mail atar. Alternatif (scope kontrolü yazmak) seçilmedi: `ROLE_MATRIX` §4'te partner'ın BÜTÜN hücreleri `HOLD`, aktif yetkisi olmayan bir role portföy altyapısı yazmak verilmemiş bir ürün kararını koda gömmek olurdu. `contract_expiry` DEĞİŞMEDİ — partner'a gitmesi kabul edilmiş yaşayan bir istisna, ve **bir istisna yeni bir yüzeyi aynı role otomatik açmaz** (ayrım kodda `includePartners` bayrağıyla taşınıyor). Partner'a atanmış geciken görev sahipsiz muamelesi görür, yöneticiye gider. **SESSİZ BAŞARISIZLIK YOLU BULUNDU VE KISMEN KAPATILDI:** `contract_expiry_emails_sent`'in flag AÇIK olduğu söylenen dört aylık dönemde sıfır satır taşıdığı **BİLDİRİLDİ** (tek prod raporu; `CHANGELOG.md:38` aynı tablo için "2 satır" diyor — **çelişki kapatılmadı**). Kesin olan tek ölçüm apply anına ait `0/0` ve o tek bir andır. Aşağıdaki kod yolu, sayı hangisi olursa olsun geçerli: Kodda açıklaması var — `sendEmail`, `RESEND_API_KEY` yoksa FIRLATMAZ, `{ok:false}` döner (`resend-transport.ts:51`); damga atılır, gönderim başarısız olur, damga ROLLBACK edilir ve net sonuç "hiçbir şey olmamış" gibi görünür. Hata `result.errors`'a yazılıp loglanır ama cron **HTTP 200 döner** → Vercel paneli "başarılı" gösterir, Hobby planında log saklanmadığı için iz kalmaz. Ayrıca yedi erken `return` yolunun hepsi damga atmadan çıkar, yani **boş tablo tek başına "hiç çalışmadı" DEMEK DEĞİL**. Düzeltme: yeni cron ucu, hiç mail gönderilmediği HÂLDE hata varsa **HTTP 500** döner (kısmi başarı 200 kalır — bir tip düştü diye tümü kırmızıya çevrilmez). Kalan açık: hata nedeni hâlâ yalnız logda; kalıcı çözüm bir koşu özeti defteri, B'nin kapsamı değil. **⚠ Bu soru cevaplanmadan yeni flag AÇILMAMALI** — migration apply güvenli (backfill 0 satırda no-op) ama flag açılırsa aynı sessizlik dört tipte birden tekrarlanabilir. **FLAG + REDEPLOY AYRILMAZ İKİLİ (ölçüldü):** env'de `BPS_CONTRACT_EXPIRY_EMAIL_ENABLED=false` yapmak yetmedi, çalışan deployment eski değeri taşımaya devam etti; redeploy şarttı. Bugün kapatılan `updated_at` kolonu + trigger sınıfının kardeşi: yarısını yapmak, işi yapmış görünüp yapmamak. Env panelindeki değer bir niyet beyanıdır, çalışan davranış değil. **`npm run qa:unit` EKLENDİ** — `isIsoDate` için 12 vaka + kopya↔kaynak eşitlik kontrolü; Codex haklı olarak "7/7 ölçüm regresyon testi değil" demişti. İkisi de negatif test edildi (round-trip kaldırılınca ve kaynak değişince kırmızıya döndü). Cron tek uçta toplandı (`/api/cron/notifications`), eski uç silindi, `vercel.json` güncellendi; flag `BPS_NOTIFICATION_EMAILS_ENABLED` (varsayılan kapalı). `contract_expiry_emails_sent` EMEKLİ ama DROP EDİLMEDİ; damgaları migration içinde `ON CONFLICT DO NOTHING` ile **backfill** ediliyor. Satır sayısı hâlâ KESİN DEĞİL — bir prod raporu "0 satır" dedi, `CHANGELOG.md:38` ise "2 satır, 23505 doğru skip etti" diyor; **çelişki kapatılmadı, karar sayımdan bağımsız hale getirildi.** Düşürmek ayrı karar (yanlış taraftaysak veri gider). **Migration prod'a UYGULANDI ve 7/7 doğrulandı** (2026-08-27: 6 kolon · RLS true · policy 0 · 2 index · backfill `yeni(0) >= eski(0)` — **bu APPLY ANINA ait tek ölçümdür**; eski tablonun dört aylık dönemde kaç satır taşıdığı hâlâ TARTIŞMALI (bir prod raporu `0`, `CHANGELOG.md:38` `2` diyor) ve backfill bilerek bu cevaba bağlı OLMAYACAK şekilde kuruldu), defter 32 → 33. **Kod commit edildi ama PUSH EDİLMEDİ** — Codex HOLD'unda. |
| 2026-08-27 | `02_rules/ROLE_MATRIX.md`, `01_product/TASK_ROADMAP.md`, `src/app/(main)/dashboard/page.tsx` (yorum) | **Duyurular görünürlüğündeki "çelişki" ölçüldü ve çelişki OLMADIĞI çıktı — kusur katman belirsizliğiymiş.** Prod ölçümü: `announcements_select` ve `critical_dates_select`, ikisi de `muhasebe`'ye okuma VERİYOR. Yani gizleme yalnızca UI'da ve RLS ile kod arasında bir ayrışma yok — o ayrım baştan bilinçliydi (güvenlik sınırı RLS'te, ürün kararı UI'da). Asıl kusur, hiçbir ifadenin hangi KATMANI tarif ettiğini söylememesiydi: "tüm roller görür" cümlesi RLS için doğru, UI için yanlış, ve katman yazılmadığı için ikisi de metne dayanabiliyordu. Üç kaynağın hiçbiri yanlış değildi, üçü de eksikti. **Düzeltme, hiçbirini "yanlış" ilan etmeden:** üçü de katman belirtilerek yeniden yazıldı — RLS altı rol okur (tenant içinde), yazma yalnız `yonetici`, `UPDATE` policy'si yok; UI `muhasebe`'ye kartı göstermez, bu bir ÜRÜN kararıdır (finansal yüzeyle sınırlı bakım aktörü vs operasyonel sinyal), güvenlik kararı değildir. Kalıcı kural `ROLE_MATRIX`'e girdi: **erişim ifadeleri katman belirtmek zorundadır.** `TASK_ROADMAP:393`'teki Batch 10 Phase 2 kaydı da katmanlı hale getirildi. | **Davranış DEĞİŞMEDİ** — tek satır kod mantığı, tek policy dokunulmadı; değişen yalnız üç metin ve bir yorum. Ürün kararı ileride tersine dönerse RLS'e dokunulmaz, yalnız UI koşulu kalkar. Ayrıca `00_core`/`01_product`/`02_rules` genelinde "tüm roller / all roles / hiçbir rol" taraması yapıldı: üç aday, ikisi elendi (`REVIEW_STANDARD.md:78` kural cümlesi — yanlış pozitif; Kritik Tarihler `SCREEN_SPEC:13`+`CHANGELOG:74` katmansız ama zararsız, çünkü orada UI ile RLS aynı şeyi söylüyor). **Taramanın verdiği kural: katman belirsizliği ancak UI, RLS'ten DAR olduğunda zarara dönüşür.** Bugün ayrışan tek yüzey Duyurular ve o da katmanlı yazıldı → ayrı açık kalem AÇILMADI. |
| 2026-08-27 | `supabase/migrations/20260827000100_create_announcements.sql` (YENİ), `src/lib/{supabase,services}/announcements.ts` (YENİ), `src/app/(main)/dashboard/actions.ts` (YENİ), `src/app/(main)/dashboard/page.tsx`, `src/types/database.types.ts`, `01_product/TASK_ROADMAP.md` (`c78774b`) + üç migration dosyasında yorum düzeltmesi (`95a90ed`) | **Batch 10 Phase 2 `Duyurular` şeridi gerçek truth'a bağlandı.** Kart Batch 10 kapanışından beri `EmptyState` gösteriyordu; şekil orada kilitlenmişti (Dashboard-only, yönetici-authored, tek yönlü, reply/reaction/recipient/read-state yok), bu yüzden yeni kapsam açılmadı. `announcements` tablosu prod `critical_dates` deseninin birebir aynısı: kurumsal seviye, `company_id` yok, her policy'de rol kapısı + tenant kapısı, `TO` yazılmamış (`={public}`). Desen varsayılmadı — dosya yazılmadan önce canlı `pg_policies` okumasından alındı; dayandığı iki fonksiyon (`current_user_active_tenant`, `current_user_role`) yalnız prod'da, hiçbir repo migration'ında yok. **Üç bilinçli sapma:** (1) tek `body` kolonu, başlık yok — şerit tek satır; (2) `updated_at` kolonu YOK, dolayısıyla trigger da yok — damgalanacak edit yolu yok, ve kolon ileride ASLA trigger'sız gelmeyecek; (3) DELETE policy VAR ama UPDATE policy YOK — yanlış duyuru kaldırılabilmeli, ama yerinde düzeltme DB sınırında kapalı (RLS varsayılanı deny). Migration **koddan ÖNCE** uygulandı ve dört sorguyla doğrulandı (5 kolon · RLS true · **tam 3 policy, UPDATE'in yokluğu tam sayımla kanıtlandı** · 2 index); defter 31 → 32. Ayrıca üç migration dosyası uygulandıkları halde `NOT APPLIED` diyordu — yorum-only düzeltildi. | **KISMİ SMOKE — tam doğrulandı DEĞİL.** 5 maddeden 3'ü canlıda geçti (yayınlama: tenant + created_by doğru, tarih 27.08.2026; silme: şerit yenilendi; limit: 4 duyurudan yalnız son 3'ü göründü), artık sıfır (`announcements` = 0 satır). **Rol görünürlüğü (`operasyon` şeridi görür ama yazamaz · `muhasebe`'de kart hiç yok) canlıda DENENEMEDİ** — seed hesaplarla (`@bps.local`) oturum açılamıyor. Kodda ve RLS'te doğrulanmış, canlıda değil. Bu boşluk Duyurular'a özgü değil: **Step 3'ün rol modeli değişimi ve tek-RLS-yeniden-yazımı da aynı nedenle rol-bazlı smoke edilemez** — seed hesap temizliği o iş başlamadan çözülmeli. Deploy sırası tutuldu: migration önce gitti, PGRST204 riski hiç doğmadı. Görünürlükte üç kaynak çelişiyor (kod `muhasebe`'ye gizliyor · `CHANGELOG.md:96` "tüm roller" · `ROLE_MATRIX.md:351` "otomatik açılmaz"); kod mevcut davranışını korudu, karar `TASK_ROADMAP:729`'da açık bırakıldı. Bildirimler (C) hâlâ ayrı ve açık. |
| 2026-07-22 | `src/types/database.types.ts`, `src/context/AuthContext.tsx`, `src/app/(main)/luca-import/page.tsx` (`0255cf9`, `415d67b`, `9b449e9`) | Üç yapısal kapanış. (1) `tenant_id`, `contracts` + `documents` Insert tiplerinde **zorunlu** yapıldı (`companies` zaten öyleydi); opsiyonel tipleme `createContract`'ın tenant_id'siz shiplenmesine izin vermiş, UI sözleşme oluşturma runtime'da kırıkmış — artık compile-time'da yakalanıyor. Update şekilleri opsiyonel kaldı. (2) Client rolü artık `user_metadata.role` yerine **`current_user_role()` RPC'sinden** (= `profiles.role`) — server/RLS ile aynı kaynak. Rol async çözüldüğü için `role` state; `loading` yalnız İLK çözümü gate'ler (token refresh flash yapmaz); monotonic seq guard eski çözümün yeni auth olayını ezmesini engeller; RPC null/error → `goruntuleyici` (fail-closed, server davranışıyla tutarlı). **Rol koşulları değişmedi, yalnız kaynak.** (3) `xlsx@0.18.5` HIGH advisory'leri (prototype pollution + ReDoS; npm'de fix yok — `latest` de 0.18.5, SheetJS yalnız kendi CDN'ine yayınlıyor) **kabul edilen risk** olarak kapatıldı: tek çağrı yeri (`mizan-parser.ts`), yalnız tarayıcı, yönetici-only, admin'in kendi seçtiği dosya. Analizin bulduğu tek gerçek eksik — yükleme yolunda boyut tavanı yokluğu — 10 MB ile kapatıldı. | İki yapısal drift kalıcı kapandı: tenant_id artık derleyici garantisi, rol tek kaynaktan. xlsx gerekçeli karara bağlandı (CDN dağıtımı reddedildi: düşük pratik riski build-time tedarik zinciri kırılganlığıyla takas etmemek için). Codex: A / B PASS. **Rol kaynağı değişimi deploy sonrası smoke gerektirir.** |
| 2026-07-14 | `src/app/(main)/**` 11 sayfa, `supabase/migrations/20260713000200_revoke_complete_appointment_atomic_execute.sql` (YENİ) (`07f3a79`, `a55543f`) | (1) **Loading-race flash fix:** `AuthContext` rolü çözülene kadar `goruntuleyici` default'una düşüyor, tam-sayfa rol gate'leri ise `loading`'i kontrol etmiyordu → yönetici açılışta ~2-3 sn **"Erişim kısıtlı"** görüyordu. 11 full-page gate (`gorevler`, `talepler`, `sozlesmeler`, `sozlesmeler/[id]`, `finansal-ozet`, `ayarlar`, `aktif-isgucu`, `randevular`, `evraklar`, `import`, `luca-import`) `authLoading`'e bağlandı; auth çözülene kadar nötr "Yükleniyor…" gösteriliyor. Rol mantığı değişmedi. Regresyon **değildi** — pre-existing race (kaynak `39b4df7`, gate'ler `b5e0dba`/`c7263b0`). Tarayıcı smoke ile doğrulandı (t=0 "Yükleniyor…", flash yok). (2) `complete_appointment_atomic` RPC `SECURITY DEFINER` + hiç GRANT/REVOKE yok → EXECUTE default'u PUBLIC; RLS bypass + scope kontrolü yok ⇒ herhangi authed kullanıcı herhangi randevuyu tamamlayıp herhangi firmaya görev insert edebilirdi. App'te sıfır çağıran. Migration **yazıldı, UYGULANMADI**. | Kullanıcıya "yetkiniz yok" diye yalan söyleyen 3 saniyelik pencere kapandı (ofis-içi kullanımda güven kırıcıydı). RPC exposure'ı için apply-hazır migration; kör DDL yok — apply öncesi `information_schema.role_routine_grants` teyidi şart. Codex: A / B PASS. |
| 2026-07-13 | Repo geneli (`865ecfb`), `src/app/(main)/{randevular,gorevler,talepler}/actions.ts` (YENİ) + `src/lib/services/appointments.ts` (`9370032`), `src/app/api/access-request/route.ts` (YENİ) + `src/app/login/page.tsx` + `supabase/migrations/20260713000100_close_access_requests_public_insert.sql` (YENİ) (`c87ca50`) | (1) **Full-codebase audit sweep:** 4 boyutta (server actions/API, service katmanı, UI, import/parsing) denetim; 30+ doğrulanmış kusur düzeltildi — UI'dan sözleşme oluşturma tamamen kırıktı (`tenant_id` eksik), görev yeniden-atama gate'i fail-open'dı, contact mutation'ları hedef kaydı firmaya bağlamıyordu, login `returnTo` open-redirect'i, middleware redirect'leri yenilenen session cookie'lerini düşürüyordu, cron auth'u feature-flag'den sonraydı, CSV parser Türkçe Excel'in `;` ayracını ve tırnak-içi satır sonlarını bozuyordu, Luca kısmi sayı parse'ı sessizce yanlış bakiye üretiyordu, `formatTRY` ondalık bozuyordu, randevu tamamlama idempotent değildi. `npm audit fix` → Next 15.5.20 (middleware bypass CVE'leri), 12 → 3 açık. (2) **Passive-company guard 6/6:** randevu/görev/talep create'leri server action'a alındı, guard insert'ten ÖNCE; `completeAppointment`'ta tamamlama pasif firmada serbest ama bağlı görev side-effect'i guard'lı (atlanınca kullanıcıya amber bildirim). Rol guard eklenmedi — RLS otorite. (3) **access_requests** login'den tarayıcı anon insert'i ile yazılıyordu (kimliksiz yazma yüzeyi) → `/api/access-request` server route'una taşındı (honeypot + rate limit + validation + service-role), `demo_requests` emsali birebir. Anon insert'i kapatan migration **yazıldı, UYGULANMADI**. | Ürünün en görünür kırığı (sözleşme oluşturma) ve iki fail-open yetki kapısı kapandı; import hattı Türkçe Excel gerçekliğiyle uyumlandı; kimliksiz yazma yüzeyi kod tarafında kapatıldı. Passive guard hattı 3/6 → **6/6**. Codex: B PASS (her batch). **İki migration prod main'de inert — apply kullanıcı gate'inde.** |
| 2026-06-05 | `02_rules/ROLE_MATRIX.md` (`f36b4e6`), `scripts/qa-static.mjs` + `qa/` (YENİ) (`736dc09`) | (1) **ROLE_MATRIX Faz A — Partner FROZEN/HOLD:** partner artık aktif mutation rolü değil; §3.3 rol amacı + odak listesi tarihsel/stale işaretlendi, §4 matriste partner sütununun tüm hücreleri `HOLD` / `HOLD (read-only kalırsa Faz B)` / `Hayır` (canlı istisna bırakılmadı), §5.x prose yönetici-current-truth'a çevrildi, yeni §10 Future roles (Ortak Yönetici/Asistan = planlama, implement değil), §11 **RLS drift risk memo** (UI/action'da partner donduruldu ama DB RLS partner-scope politikaları canlı olabilir → Faz E, Tier-1), §12 Open questions. Mutation davranışının source-of-truth'u **kod** (contact create = yönetici-only). (2) **QA mini harness:** `npm run qa:static` — 11 statik kural (service_role confinement, partner-HOLD, guard ordering, payload disiplini), read-only SQL baseline + manuel browser smoke checklist. Yeni bağımlılık yok. | Rol modeli kararı yazılı hale geldi ve kod ile doc arasındaki partner mismatch'i kapandı; RLS drift'i kaybolmak yerine kayda geçti. Harness, sonraki batch'lerde regresyon kapısı olarak kullanıldı (26 dosyalık audit sweep'i 0 FAIL ile geçti). |
| 2026-04-25 | `supabase/migrations/20260425000100_documents_read_boundary_alignment.sql` (YENİ) | Documents read-boundary trust alignment: `documents_select` ve `documents_bucket_select` policy'lerinden muhasebe + görüntüleyici çıkarıldı (DROP + CREATE). Allowed read list `('yonetici', 'operasyon', 'ik')` + partner scope branch (unchanged). Path-format CASE guard verbatim korundu. INSERT/UPDATE/DELETE policy'leri ve UI dokunulmadı (UI zaten muhasebe + görüntüleyici için EmptyState gösteriyordu). 2026-04-23 storage foundation batch'inin documents tablosundan miras aldığı drift de aynı migration ile kapandı. **Trust alignment patch: GREEN. Post-apply runtime verification: required. Known follow-up: UX/read-denied vs zero-data clarity.** | ROLE_MATRIX §4:307 ile backend RLS arasındaki "muhasebe+görüntüleyici evrak görüntüleyemez" mismatch'i hard-kapatıldı; UI zaten kaynağa hizalı, backend artık o güveni de taşıyor. |
| 2026-04-22 | `docs/runbooks/user-offboarding.md` (YENİ) + `00_core/SECURITY_CHECKLIST.md` §1.5 | User offboarding runbook eklendi: 5-adım prose prosedür (auth disable → profile soft-delete pending → scope null-out → contracts.responsible reassign → Obsidian audit stamp). Prensipler: disable-not-delete, historical data preservation, scope kesimi, display reference reassign. Profile soft-delete schema dependency explicit flag'li. Post-offboarding verification checklist'i dahil. SECURITY_CHECKLIST §1.5 RED → YELLOW (runbook var, drill yok). Hiçbir kod/schema/RLS değişmedi. | ChatGPT parallel review P1 batch: auth hardening 'içeri girmesin'i kapattı, şimdi 'çıktıysa gerçekten çıktı mı' prosedürü yazılı hale geldi. İlk gerçek offboarding öncesi hazır. |
| 2026-04-22 | `src/app/api/cron/contract-expiry/route.ts` | Handler'a `expected_in_window` companion log eklendi: independent service_role COUNT(*) pencere sorgusu (status=aktif, end_date ∈ [today, today+30]), expanded summary log (`expected_in_window=X evaluated_actual=Y ...`), anomaly warning (expected != evaluated → "Verify SUPABASE_SERVICE_ROLE_KEY via /api/healthz"), response body'ye `expectedInWindow` alanı eklendi. Silent zero-condition (pencere boş mu, auth bozuk mu) ayrımı runtime'da görünür. Advisory-only: countError abort etmez. No schema/RLS/trigger/threshold/template/recipient/sender/cron-time change. +25 satır. | 20 Nisan tipi silent failure artık log diff ile 1 invocation'da yakalanabilir; 3.5 healthz endpoint'i ile complementary (3.5 deploy-time, 7.4 runtime). SECURITY_CHECKLIST 7.4 RED → GREEN. |
| 2026-04-22 | `src/app/api/healthz/route.ts` + middleware public-route update | Healthz env var value-correctness runtime self-check endpoint eklendi. JWT decode + role assertion + query sanity + CRON_SECRET + RESEND format kontrolleri. Bearer auth (CRON_SECRET). 20 Nisan silent failure incident'ının forward-looking guarantee'si. `src/lib/supabase/middleware.ts` public-route listesine `/api/healthz` eklendi (aynı pattern: cron rotaları gibi handler-level bearer auth yeterli). Side-effect yok; RLS, schema, cron, env var dokunulmadı. | Env var slot karışıklıkları deploy sonrası 1-shot curl ile tespit edilebilir (4 gün → 4 saniye). SECURITY_CHECKLIST 3.5 RED → GREEN. |
| 2026-04-22 | Rotation production validation (smoke test) | Vercel Cron Jobs panel Run butonu ile manuel cron tetiklendi: `POST /api/v1/.../crons/run` → 200 OK (yeni CRON_SECRET ile handler authenticated ✅). Supabase `contract_expiry_emails_sent` tablosu değişmedi (2 satır, idempotency `23505` doğru skip etti, write-first-then-send pattern gereği Resend'e hiç gidilmedi). Resend v2 key "No activity" kaldı — bu **beklenen** davranış (handler idempotency skip nedeniyle Resend API'ye çağrı yapmadı). Yeni RESEND_API_KEY value Vercel'de live ve formatı doğru (prefix `re_Vm`, length 36); end-to-end send doğrulaması yeni bir contract/recipient gerektiriyor ama transport zaten çalıştığı biliniyor (Pazar 2 email başarıyla gönderildi, sadece credential değişti). Hiçbir kod, migration, RLS değişikliği yok; burn-in clock korundu. Bonus bulgu: Vercel Hobby plan log retention 1 saat — 3.5 healthz endpoint maddesinin motivasyonunu güçlendiriyor. | Katman 1.5 rotation batch'i production'da fiilen doğrulandı; yeni secret'lar live ve çalışıyor; handler auth + idempotency pattern'i production'da teyit edildi |
| 2026-04-21 | Katman 1.5 paranoia rotation batch tamamlandı | CRON_SECRET ve RESEND_API_KEY rotate edildi (browser-side Web Crypto + Resend Create API Key). Vercel deployments: `AQLmAHw96` Production Ready (RESEND için) + `GHNSpxsfo` Production Ready (CRON_SECRET için, dünden). Eski Resend key (`re_13RmBSJp...`, 7 gün önce) Resend dashboard'dan delete edildi. Vercel'deki "Need To Rotate" badge'leri ikisi için de kayboldu. Cross-tab clipboard bridge tekniği kullanıldı (Resend dialog → temporary textarea + native cmd+C → Vercel paste). SECURITY_CHECKLIST 4.4 RED → GREEN, kanıt tablosu eklendi. Yeni discipline yazılı hale getirildi: secret değerleri ASLA markdown/handoff/commit message'da plain-text yer almaz. Hiçbir kod, migration, RLS değişikliği yok; burn-in clock korundu (trigger/threshold/template/recipient/sender/cron-time'a dokunulmadı). | İki secret leak yüzeyi kapatıldı; secret hijyen disiplini kalıcı hale getirildi; rotation pattern dökumante edildi (cross-tab clipboard bridge gelecek için tekrar kullanılabilir teknik) |
| 2026-04-20 | Vercel env scope full sweep + Need-To-Rotate bulgusu | Tüm 8 env var'ın value-tipi sweep'i (JWT decode, URL match, format identification): tüm scope'lar ve değer tipleri doğru, başka silent failure yüzeyi yok. Vercel/GitHub Secret Scanning otomatik tespit: `RESEND_API_KEY` ve `CRON_SECRET` "Need To Rotate" badge — büyük olasılıkla CRON_SECRET'in handoff dokümanlarında plain-text geçmesinden. Yeni discipline: secret değerleri markdown'larda asla plain-text yer almasın, partial mask kullan. SECURITY_CHECKLIST.md'de 4.4 maddesi RED açıldı, Katman 1.5 P0 paranoia rotation batch sıraya alındı. Production anon key sonunda trailing `\n` yan-bulgu (Vercel build trim ediyor, çalışıyor; rotation turunda doğal olarak temizlenir). SUPABASE_SERVICE_ROLE_KEY için Sensitive toggle denendi → Vercel "Unchanged Value" uyarısı → cancel → rotation batch'ine ertelendi. | Env value tipi sweep tamamlandı, başka silent failure yüzeyi yok; iki secret rotation P0 sıraya girdi; secret hijyen disiplini yazılı hale geldi |
| 2026-04-20 | `SECURITY_CHECKLIST.md` eklendi (ops review aracı, source-of-truth değil) | Furkan tarafından şablon olarak verilen güvenlik kontrol listesi, 20 Nisan incident delta'sı işlenerek ve iki yeni madde eklenerek dokumante edildi: `3.5 Env var value-correctness runtime self-check` ve `7.4 Silent zero-condition disambiguation`. Disposition: ops review aracı; active rules WORKFLOW_RULES ve ROLE_MATRIX'te yaşamaya devam eder. Öneri yer: `00_core/SECURITY_CHECKLIST.md`. README doc map'e eklenmeli; CODEX'e eklenmeyecek. | Sistemik güvenlik posture'unun forward-looking review aracı; incident-driven iyileştirmenin kalıcı kayıtlı hale gelmesi |
| 2026-04-20 | Contract Expiry Email V1 silent failure resolution (Vercel env var only) | `SUPABASE_SERVICE_ROLE_KEY` (Production) slot'undaki anon JWT gerçek service_role JWT ile değiştirildi; deployment `5k4nr3481` redeployed no-cache, mevcut main commit `0271f37` üzerine. Curl doğrulaması: `evaluated=1, attempted=2, sent=2` (önceki state: `0/0/0` 4 gün boyunca). `contract_expiry_emails_sent` tablosuna 2 satır yazıldı, idempotency 2. call'da `23505` ile skip etti (doğru davranış). Hiçbir kod, migration, RLS değişikliği yapılmadı. | 4 günlük silent cron failure kapandı; burn-in clock restart 22:57 TR; Weekly Digest açılma önkoşulu (clean 4-day observation) yeniden saymaya başladı |
| 2026-04-24 | `supabase/migrations/20260423000100_documents_storage_policies.sql` + `src/components/modals/UploadDocumentModal.tsx` + `src/app/(main)/evraklar/page.tsx` | Storage Foundation V1.1 shipped (commit `51be6a1`): documents bucket RLS policies (SELECT/INSERT/DELETE) with deterministic CASE guard, PDF MIME + 10MB client validation, storage-first→DB-second orchestration, 60s signed URL indir, partner UI drift correction (canMutateEvrak now includes partner). Runtime validation: 2/5 PASS demo + 2/2 PASS prod smoke. | Foundation for Hafta 2 Contract PDF batch; documents module upload/download now live on bpsys.net |
| 2026-04-24 | Environment strategy repositioning (demo Supabase → bps-dev) | `BPS-Demo` project renamed to `bps-dev`; `.env.local` cleanup (DEMO_SUPABASE_*, DEMO_PROJECT_MARKER, DEMO_USER_PASSWORD removed, 3 lines kept); test remnants cleaned; AuthContext debt note + environment karar notu written to Vault. Service role rotate deferred (dev key, low risk, high cost). See `Vault77/01-projects/bps/decisions/karar-bps-environment-strategy.md`. | Resolves dev-vs-sales-demo dual-use friction; localhost now bound to dev-purposed project; sales demo posture rules documented for future need |
| 2026-04-24 | B&P first-user adoption signal captured | Secretary WhatsApp daily appointment message format observed; analyzed as workflow-truth signal (not commercial truth). Recorded as signal-only (not batch, not decision) per sinyal-before-scope discipline. 6 validation questions to answer before batch slot allocation. See `Vault77/01-projects/bps/notes/not-bp-sekreter-sinyali-gunluk-randevu-mesaji.md`. | Adoption wedge candidate for Hafta 3 (post-Contract PDF + tenant foundation); explicit batch-gating criteria |
| 2026-04-22 | Hafta 1 prioritization + Testing Infra Gate + Track B Scheduling Gate (`01_product/TASK_ROADMAP.md`) | 7 kalemlik triage tablosu MUST/SHOULD/DEFER etiketli yazılı dondu. İki operasyonel gate eklendi: Testing Infra Gate (Hafta 2 multi-tenant foundation öncesi 4 çıktı zorunlu — chosen tool, test scope, execution kararı, 1 executable smoke path) ve Track B Scheduling Gate (Pazar gece 2 görüşme slot lock, Pazartesi 12:00 kontrol, slot yoksa SHOULD düşer). Düşme sırası önceden tanımlı (#4 MODULE_SYSTEM_SPEC → #3 Consulting scope); MUST düşemez. Track B metrik düzeltmesi: saat → görüşme sayısı. Detay `Vault77/01-projects/bps/haftalik/hafta-1-prioritization.md`. | Plan disiplinini governance katmanına yedirir; teknik yoğunluğa Track B'nin sessiz yenilmesini yapısal olarak engeller; Hafta 2 multi-tenant foundation için ön koşul netleştirir |
| 2026-04-15 | Katman 2 output slices + Katman 1 residual closure (`src/lib/draft-hotel-email.ts`, `src/app/(main)/dashboard/page.tsx`, `src/app/(main)/finansal-ozet/page.tsx`, `src/app/(main)/kurumsal-tarihler/page.tsx`, `src/app/globals.css`, `src/components/shell/Layout.tsx`, `src/components/ui/PageHeader.tsx`, `src/lib/email/*`, `src/app/api/cron/contract-expiry/route.ts`, `supabase/migrations/20260415000500_contract_expiry_emails_sent.sql`, `vercel.json`, `src/types/database.types.ts`) | Closed Katman 1 Geçiş ve Güven residual (draft-hotel-email mock dependency removed, dashboard workforce fetched from real truth — `src/app/` runtime `@/mocks` imports effectively sıfır). Shipped Katman 2 Geri Çağırma ve Çıktı first output slices: Finansal Özet PDF Export V1 (yonetici-only, snapshot-of-screen, `window.print()` + print CSS, print-only timestamp, no new data model) with authorized-role-gated read hardening; Kurumsal Kritik Tarihler PDF Export V1 (same pattern, narrow second surface, `+42/-3` one file). Landed shared print infrastructure on main as a dedicated commit (globals.css `@media print` block, Layout.tsx shell chrome `print:hidden` wrapping, PageHeader.tsx actions `print:hidden`, main content offset reset in print). Shipped Katman 2 recall first slice: Contract Expiry Email Recall V1 — daily batched cron (`/api/cron/contract-expiry` via Vercel Cron), yonetici global + partner scoped via `partner_company_assignments`, 30-day single threshold aligned with `getApproachingLevel("approaching")`, `contracts.responsible` display-only never routing, per-(contract, recipient, threshold) idempotency via new `contract_expiry_emails_sent` table (service-role-only, RLS enabled with zero user policies), Resend REST transport with no new npm dependency, bearer-auth via `CRON_SECRET`, feature-flag default-disabled, marketing-free Turkish template with neutral recall wording. | Unblocks organizational value flow out of BPS (management PDF artifacts) and recall back into BPS (critical contract-expiry event alerts). Katman 1 güven temeli kapatıldı; Katman 2 aktif. Recall slice is code-shipped but ops-gated: live enablement requires Vercel env vars (`RESEND_API_KEY`, `CRON_SECRET`, `BPS_CONTRACT_EXPIRY_EMAIL_ENABLED`), DKIM/SPF/DMARC on bpsys.net, Resend domain verification, and migration apply on production + demo Supabase. Weekly Digest direction is framed (3-block V1, not 4-block) but implementation is not opened — sequencing-blocked on event-email pilot burn-in. |
| 2026-04-15 | Strategic pivot + docs alignment (03_strategy/BPS_YAPILANMA_PAKETI.md, 00_core/README.md, 00_core/CODEX.md, CLAUDE.md, 01_product/TASK_ROADMAP.md, 00_core/SESSION_HANDOFFS.md) | Added the BPS Yapılanma Paketi as the new strategic direction document and aligned the core governance/docs layer to the updated framing: firma-merkezli service operations platform, kişi-merkezli günlük deneyim, clarified natural growth areas (firma-merkezli time tracking, firma-bazlı ekonomik görünürlük, dar pipeline), preserved source-of-truth execution hierarchy, and recorded the strategic pivot session in handoffs. | Establishes the new strategic direction without rewriting execution history or weakening active governance; aligns product definition, roadmap reading, and agent steering around the same framing. |
| 2026-04-15 | Pre-launch mock audit pushdown + Dashboard truth correction (`src/app/(main)/ayarlar/page.tsx`, `src/app/login/page.tsx`, `src/components/shell/Topbar.tsx`, `src/lib/format-currency.ts`, `src/app/(main)/sozlesmeler/page.tsx`, `src/app/(main)/sozlesmeler/[id]/page.tsx`, `src/app/(main)/dashboard/page.tsx`) | Shipped Faz 1A-1C and Faz 2A/2A.1/2A.2. Ayarlar Kullanıcılar reads real `profiles`; Erişim Talebi simplified to Ad Soyad + E-posta with silent `diger` default; Topbar dead global search removed (honest absence); contract amounts formatted as `₺X.XXX,XX` via new `formatTRY` helper (display-only, storage unchanged). Dashboard top 6 KPI cards wired to real `companies` / `contracts` / `staffing_demands` / `workforce_summary` / `tasks` / `appointments`; Dashboard signal cards (Bugünün Görevleri, Açık Personel Talepleri, Yaklaşan Sözleşme Bitişleri, Eksik / Süresi Dolan Evraklar) wired to real truth via existing service readers with honest loading + empty states. Remaining Dashboard blocks (Riskli Firmalar, Kritik Tarihler card, HotelEmailDraftHelper, İnisiyatifler, Duyurular, ActivityFeed) unchanged. | Closes the most visible pre-launch trust holes surfaced by the mock audit; Dashboard decision surface now reflects RLS-scoped real truth end-to-end across KPIs and the four paired signal cards. |
| 2026-04-15 | Evre 1 closeout (`01_product/TASK_ROADMAP.md`, `00_core/CHANGELOG.md`) | Closed Evre 1 operationally. Shipped surfaces added: Luca downstream derived visibility (`derive_financial_summaries_from_mizan` RPC → `financial_summaries.open_receivable` → Firma Detay > Ticari Özet) and Luca source signal (minimal `last_source` on `financial_summaries`, stamped by both `confirm_financial_data` and `derive_financial_summaries_from_mizan`, surfaced as a muted caption on the existing Ticari Özet card). Demo preview environment running on `demo-preview` branch at `bps-git-demo-preview-furkanyahsi-1537s-projects.vercel.app` with Vercel Authentication disabled for preview (BPS own auth is sufficient); demo credentials are managed out-of-repo, not recorded in docs. Preview access blocker resolved at deployment/ops layer (no code change). Rehearsal result: 6/7 PASS + 1 known WARN (sector-setup limitation). Known later-decision items recorded but not activated: Luca is_overdue coherence, Luca stale carryover, Finansal Özet parity, sector-setup WARN. | Records honest Evre 1 closure without opening Evre 1.1, Evre 2, or new modules; preserves source-of-truth discipline; documents demo preview access path and operational resolution |
| 2026-04-15 | Post-migration docs truth sync (TASK_ROADMAP, ARCHITECTURE, PRODUCT_STRUCTURE) | Migration state updated to reflect Faz 0-7 completed. Partner scope wording changed from future-tense to shipped. Shipped surfaces documented: public landing, demo request, Excel Import V1, Luca Mizan Import V1, Sector Templates V1, company surface trust polish | Restores docs-product truth parity without roadmap widening or strategy rewrite |
| 2026-04-13 | Agent steering + worktree ops docs | Added repo-root `CLAUDE.md`, `docs/policies/BPS_WORKTREE_POLICY.md`, and `docs/runbooks/git-worktree-setup.md` | Separates agent-entry, worktree-policy, and technical-runbook layers without changing product source-of-truth |
| 2026-04-13 | Stratejik pivot + sektor teknoloji haritasi | BPS "Connect don't Replace" stratejisi, 8 sektor teknoloji haritasi, Firma Saglik Skoru, sektor sablonlari, entegrasyon oncelik matrisi. Evre 1 scope guncellendi. Yeni strateji dosyalari eklendi: BPS_STRATEGIC_PIVOT, BPS_ORBIS_REVIEW_GAINS, PROJECT_SKILLS_AND_FRAMEWORKS, BPS_SECTOR_TECHNOLOGY_MAP | BPS'in "baglayici platform" konumlandirmasi ve sektorel derinlik plani |
| 2026-04-12 | Docs consolidation (SYSTEM_MAP->PRODUCT_STRUCTURE, COMPONENT_SYSTEM->SCREEN_SPEC, design-tokens+UI_MAPPING->DESIGN_SYSTEM, BUILD_PRIORITY/REVIEW_STANDARD/MIGRATION_SAFETY removed) | Merged 3 file pairs and removed 3 redundant files to reduce total doc count from 25 to 19 | Simplifies documentation system without losing any content; removed files' rules already covered by CODEX, SKILLS, and WORKFLOW_RULES |
| 2026-04-12 | Docs system overhaul (`README`, `CODEX`, `CHANGELOG`, `TASK_ROADMAP`, `ARCHITECTURE`, `ROLE_MATRIX`, `WORKFLOW_RULES`, `MIGRATION_SAFETY`) | Full documentation audit: SaaS pivot vision aligned in CODEX/README, migration docs archived, StaffApp references replaced with BPS, MIGRATION_SAFETY activated, folder structure reorganized (03_strategy, 04_future, 99_archive), Evre 0 completion and Evre 1 active workstream noted in TASK_ROADMAP | Aligns documentation system with post-migration SaaS pivot state without changing product behavior rules |
| 2026-04-12 | Migration completion, demo environment, Vercel deployment | Evre 0 (Real-Data Migration Faz 0-7) officially completed and closed. 14 Supabase tables, 12+ service files, all mock truth removed, RLS + partner scope everywhere. Demo environment deployed on separate Supabase project. Production verified on bpsys.net | BPS transitions from mock-backed prototype to real-data production system |
| 2026-04-12 | BPS_MODULE_ARCHITECTURE.md | Created product vision/module architecture document (v2.0) covering core omurga (10 modules), 20 yatay ek modules, 8 dikey sektorel modules, AI Insight module design, entegrasyon mimarisi, and fiyatlandirma modeli. Classification: reference/vision only — not adopted into active source-of-truth | Future commercial packaging and tenantization planning reference. Does not change current product scope, workflow rules, or role model |
| 2026-04-12 | Demo Supabase project (BPS-Demo) | Created isolated demo project (tiqemcsjuyudahgmqksw, eu-west-1, Nano $0/ay). 19 migrations pushed, demo seed 20/20 PASS, storage bucket created, auth metadata fix applied | Production-isolated demo environment for controlled sales demos |
| 2026-04-12 | Demo seed pipeline closeout | Demo seed auth metadata fix (role+unit in user_metadata), notes tag fix (ticari/operasyonel/risk/yasal → sozlesme/operasyon/odeme/evrak), all 20/20 validation checks PASS | Demo dataset ready for rehearsal use |
| 2026-04-12 | Demo rehearsal PASS | Tested yonetici (11 menu, 9 firma), Partner A (10 menu, 3 firma), Partner B (10 menu, 4 firma), Viewer (3 menu, read-only). f3 shared scope, f6 management-only scope verified | Demo environment validated for sales demo readiness |
| 2026-04-12 | Vercel env + deploy | Production (bpsys.net, dffdzbmnmnokbftbujsy) and Demo Preview (tiqemcsjuyudahgmqksw) configured with separate env variables. Both deployed successfully | Two-environment isolation: production safe, demo functional |
| 2026-04-12 | Docs disposition patch | Classified BPS_MODULE_ARCHITECTURE.md as reference/vision (not active source-of-truth). Classified BPS_Session_Update as handoff/history. No promotion of modular packaging vision into active product docs | Maintains source-of-truth integrity while recording completed work |
| 2026-04-09 | Phase 7 migration closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`) | Recorded `Faz 7 — Cutover + Mock Temizligi` as closed and green in the real-data migration sequence, with validated retirement of active mock-backed migrated readers, real `companies` truth on `Firmalar` row and `Firma Detay` shell metadata, quarantining of `MOCK_FIRMA_DETAY` / `MOCK_FIRMA_TIMELINE` as non-domain presentation/local data, and passing role/status/workflow/scope hardening plus build/typecheck | Captures completion of the real-data migration program without opening a new roadmap phase, inventing new scope, or drifting into a hidden feature batch |
| 2026-04-09 | Phase 6 migration closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`) | Recorded `Faz 6 — Dashboard + Raporlar` as closed and green in the real-data migration sequence, with validated real derived readers on `Dashboard` and `Raporlar`, partner-scope enforcement on derived surfaces, and removal of active migrated-reader dependency on `@/mocks/dashboard` and `@/mocks/raporlar` | Captures the completed derived-reader migration layer without starting Faz 7, changing migration order, introducing a new primary truth, or drifting into BI / analytics-platform behavior |
| 2026-04-09 | Phase 5 migration closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`) | Recorded `Faz 5 — Finansal Ozet` as closed and green in the real-data migration sequence, with validated real summary truth on `Finansal Ozet`, upload/review/confirm flow, finance-page KPI cards, per-company receivables distribution, `Portfoy Saglik Ozeti`, and `Firma Detay > Ticari Ozet` | Captures the completed management-visibility migration slice without starting Faz 6, changing migration order, or drifting into accounting/ERP behavior or premature Dashboard/Raporlar cutover |
| 2026-04-09 | Phase 4 migration closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`) | Recorded `Faz 4 — Evraklar + Kurumsal Kritik Tarihler` as closed and green in the real-data migration sequence, with validated real truth on `Evraklar`, document upload metadata flow, document validity update flow, `Firma Detay > Evraklar`, `Firma Detay > Genel Bakis` document-compliance/checklist reader, and `Kurumsal Kritik Tarihler` | Captures the completed document/compliance visibility migration slice without starting Faz 5, changing migration order, or drifting into drive/archive, compliance-engine, HRIS, CRM, or ERP/accounting behavior |
| 2026-04-09 | Phase 3 migration closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`) | Recorded `Faz 3 — Talepler + Randevular + Gorevler + Aktif Is Gucu` as closed and green in the real-data migration sequence, with validated real truth on `Talepler`, `Randevular`, `Gorevler`, `Firma Detay > Talepler`, `Firma Detay > Randevular`, `Firma Detay > Aktif Is Gucu`, and `Firmalar` list appointment-derived parity | Captures the completed coordinated operations migration slice without starting Faz 4, changing migration order, or drifting into HRIS, CRM, or ERP/accounting behavior |
| 2026-04-09 | Phase 2 migration closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`) | Recorded `Faz 2 — Sozlesmeler` as closed and green in the real-data migration sequence, with validated contract truth on `Firma Detay > Sozlesmeler`, `Genel Bakis > Aktif Sozlesmeler`, `Sozlesmeler` listesi, and `Firmalar` list active-contract count | Captures the completed contracts migration slice as lifecycle truth without starting Faz 3, changing migration order, or drifting into ERP/accounting or CRM behavior |
| 2026-04-09 | Workflow docs patch (`00_core/WORKFLOWS_PROMPT.md`, `00_core/CHANGELOG.md`) | Added `Workflow 5 — Migration Slice Smoke Test` standard prompt template for post-deploy migrated-slice verification covering migration status, write/persistence checks, derived readers, role/scope enforcement, mock retirement, and cross-surface parity | Fills the gap between batch closeout governance and live migrated-slice runtime verification without changing product scope or workflow rules |
| 2026-04-07 | Migration master plan docs sync (`00_core/README.md`, `00_core/CODEX.md`, `00_core/CHANGELOG.md`) | Synced the core docs system to the new `01_product/REAL_DATA_MIGRATION_MASTER_PLAN.md` file by adding it to the active product-doc map and canonical read order | Makes the real-data migration master plan part of the authoritative docs flow without changing product scope, workflow rules, or migration meaning |
| 2026-04-07 | Future note patch (`01_product/TASK_ROADMAP.md`, `00_core/CHANGELOG.md`) | Added a bounded later-planning note for `Gorev Yuku / Is Kapanis Gorunurlugu` covering task-load concentration, overdue clustering, closure/carrying visibility, and bottleneck visibility after real task truth exists | Records the accepted future note without activating a batch or drifting into employee scoring, ranking, leaderboard, HR performance management, or bonus/payroll logic |
| 2026-04-07 | Future note patch (`01_product/TASK_ROADMAP.md`, `00_core/CHANGELOG.md`) | Added a bounded later-planning note for `Gorev Talebi / Acik Ustlenilebilir Is` covering temporary open-claimable work that becomes a normal assigned task once claimed, with unit / relevant-role-pool boundaries and post-migration timing only | Records the accepted future note without activating a batch or drifting into marketplace, ticketing, cross-unit free-for-all, or second-communication-layer behavior |
| 2026-04-07 | Future note patch (`01_product/TASK_ROADMAP.md`, `00_core/CHANGELOG.md`) | Added a bounded later-planning note for `Oneri / Hata Bildir` as a lightweight internal rollout feedback intake surface covering `Hata` / `Oneri`, screen/module context, short title, description, priority, optional screenshot, and yonetici review only | Records the accepted future note without activating a new batch or module and preserves anti-helpdesk / anti-ticketing / anti-chat boundaries |
| 2026-04-07 | Role model cleanup patch (`02_rules/ROLE_MATRIX.md`, `02_rules/WORKFLOW_RULES.md`, `01_product/SCREEN_SPEC.md`, `01_product/PRODUCT_STRUCTURE.md`, `01_product/ARCHITECTURE.md`, `01_product/TASK_ROADMAP.md`, `00_core/CHANGELOG.md`) | Synced authoritative docs to the final accepted role model: `satis` removed, `partner` added as a portfolio-scoped admin-like role, `goruntuleyici` retained as bounded read-only, and authorization reframed as `rol + kapsam` rather than role-only | Makes the source-of-truth docs reflect the accepted scoped-access model without turning BPS into a partner portal, widening finance beyond portfolio scope, or inventing schema; records that real-data migration will require partner-to-portfolio/company mapping intent |
| 2026-04-05 | Pre-deploy docs sync patch | Synced TASK_ROADMAP, SCREEN_SPEC, PRODUCT_STRUCTURE, WORKFLOW_RULES, ROLE_MATRIX with accepted implementation state: muhasebe role, Notlar V1, Kurumsal Kritik Tarihler V1, Bounded Integrity Fix Batch. Added §11 Kurumsal Kritik Tarihler rules to WORKFLOW_RULES. Updated Dashboard/Firmalar component inventories. Added Kurumsal Kritik Tarihler access rows to ROLE_MATRIX. Corrected Topbar quick-add scope in PRODUCT_STRUCTURE. | Brings authoritative docs into alignment with current production implementation before deploy |
| 2026-04-05 | Kurumsal Kritik Tarihler V1 | Implemented dashboard-linked standalone critical-date visibility page at `/kurumsal-tarihler` with 9 mock records, Dashboard signal card (all roles, approaching/overdue only), yonetici-only create/edit. Not firm-scoped Evraklar. Not document management. | Company-wide critical date/deadline visibility without compliance-suite, workflow-engine, or document-management expansion |
| 2026-04-05 | Bounded Integrity Fix Batch | Closed 4 audit findings: (1) note-create header action leaked to muhasebe/goruntuleyici, (2) Firmalar quick-note dead-end path removed, (3) CommercialSummaryCard now reads confirmed financial data, (4) muhasebe contract-row navigation cues removed | Restores role-boundary integrity and cross-surface financial-data consistency |
| 2026-04-05 | Firma Notlar V1 | Activated the last Company Detail tab (Notlar) with structured notes feed, pinned section, tag filter, QuickNoteModal integration for create/edit, and Son Notlar card consistency. All 9 tabs now active. | Completes documented Company Detail surface scope without chat/CRM/timeline drift |
| 2026-04-05 | REVIEW_STANDARD muhasebe alignment fix (`02_rules/REVIEW_STANDARD.md`) | Updated §4 Role And Access Consistency Check: replaced stale "Finansal Ozet remains manager-only by default" with documented dual-actor model (yonetici owner + muhasebe bounded summary-maintenance), added summary-maintenance drift fail sign | Eliminates contradiction between REVIEW_STANDARD gatekeeping language and the accepted muhasebe role extension documented in WORKFLOW_RULES, ROLE_MATRIX, SCREEN_SPEC, and ARCHITECTURE |
| 2026-04-05 | Muhasebe docs reconciliation patch (`02_rules/WORKFLOW_RULES.md`, `02_rules/ROLE_MATRIX.md`, `01_product/SCREEN_SPEC.md`, `01_product/ARCHITECTURE.md`, `00_core/CHANGELOG.md`) | Reconciled authoritative docs with the accepted muhasebe role extension: updated financial-visibility ownership rules to allow muhasebe as bounded summary-maintenance actor (upload/review/confirm), added muhasebe role definition to ROLE_MATRIX with 6-column permission matrix, updated SCREEN_SPEC Finansal Ozet ownership language, updated ARCHITECTURE role model to 6 roles with muhasebe boundary description, and added yonlendirme cross-unit resolution clarification | Ensures governance docs accurately describe muhasebe as a bounded summary-level financial-input role without expanding BPS into accounting software, ERP, payroll, ledger, reconciliation, invoice operations, or tax workflows; clarifies that Financial Summary remains management visibility with muhasebe as the data-feeding actor and yonetici as the management owner |
| 2026-04-05 | Muhasebe role-extension implementation | Added `muhasebe` as the 6th BPS role — bounded financial-input role with access to `Finansal Ozet` (full including upload-extract-confirm), `Company Detail` (card 1 Aktif Sozlesmeler + card 6 Ticari Ozet + Yonlendirmeler + Sozlesmeler tab read-only), `Dashboard` (Toplam Firma KPI + Riskli Firmalar with ticari baski), `Raporlar` (riskli-firma + partner-ozet). Blocked from 7 operational pages. canResolve simplified: each role resolves own birim, yonetici resolves all. | Closes the workflow gap where yonetici proxied all muhasebe financial-input work. Role model expands from 5 to 6 roles. No accounting-software, ERP, payroll, or invoice-generation behavior introduced. |
| 2026-04-05 | Yetkili Kisiler closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`, `02_rules/ROLE_MATRIX.md`) | Recorded `Firma Yetkili Kisileri` as a completed bounded Company Detail enhancement, activating the `Yetkililer` tab with up to 5 firm-scoped contacts per firma, exactly one `ana yetkili`, fields `adSoyad` / `unvan` / `telefon` / `eposta` / `anaYetkili` / `kisaNotlar`, role-gated access (`yonetici` + `satis` full, `operasyon` phone/email-only edit, `ik` + `goruntuleyici` no access), and ana-yetkili-consistency derivation for draft generators | Captures the completed firm contact visibility surface without introducing a global contacts page, CRM/contact-history behavior, people directory, relationship timeline, dashboard/reporting expansion, or sector-filter work (sector filtering already existed in Firmalar Listesi and required no changes) |
| 2026-04-05 | Auth Foundation Phase 2A closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded `Auth Foundation Phase 2A — Access Request + Admin Approval Flow` as completed and production-verified, covering an inline access-request form on the login page (Ad Soyad, E-posta, Birim), a yonetici-only `Erisim Talepleri` review tab in `Ayarlar` with approve/reject lifecycle (`beklemede` / `onaylandi` / `reddedildi`), duplicate-pending prevention, and yonetici-only RLS enforcement at the data layer | Captures the completed bounded onboarding-friction reduction step without introducing open self-signup, automatic auth-user creation, invitation automation, email sending, public registration, or broader identity-management expansion; records that real user creation remains a manual Supabase dashboard step after approval and that this phase is production-verified |
| 2026-04-05 | IK role-extension closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`, `02_rules/ROLE_MATRIX.md`) | Recorded the `IK` role as the 5th BPS role through a bounded role-extension phase, covering document-compliance / personnel-completion access to `Evraklar` (full), `Aktif Is Gucu` (read-only), `Gorevler` (bounded execution, no reassignment), `Company Detail` Genel Bakis operational cards + Son Notlar + Bahsetmeler + Yonlendirmeler, and `Raporlar` is-gucu subset only | Captures the completed bounded IK role without introducing HR-software depth, recruitment, leave/bordro/performance, commercial tools, financial visibility, or management-layer access; records that `muhasebe` remains a separate future role-extension phase and that the role model expanded from 4 to 5 roles with `ik` added between `satis` and `goruntuleyici` |
| 2026-04-04 | pricing-calculator workstream closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded the full `Ticari Hesap Motoru + Parametre Seti Yonetimi` post-roadmap workstream as completed through one bounded phase with a `Company Detail`-only, `Genel Bakis`-only `Teklif Hesaplayici`, role-gated access for `yonetici` / `satis`, one hidden active parameter set, stateless reactive calculation, required inputs `Net Ucret (gunluk)` / `Hedef Kar Orani (%)`, optional secondary inputs `Ek Odeme`, `Yemek`, `Servis`, `Kiyafet`, and outputs `Tahmini Isveren Maliyeti` / `Onerilen Teklif Bedeli (KDV Haric)` | Captures the completed bounded decision-support calculator without introducing parameter-management UI, admin reference panels, effective-date logic, parameter history/versioning, payroll / accounting / ERP behavior, `Dashboard` / `Raporlar` / `Finansal Ozet` integration, quotation-document generation, saved calculation history, or free-form formula editing; records that the active parameter set remained hidden and hardcoded for the bounded phase, the tool stayed decision-support only, and the audit closed with no material findings |
| 2026-04-04 | outbound-draft workstream closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded the full `Ticari Temas / Outbound Draft Katmani` post-roadmap workstream as completed through one bounded phase with a `Company Detail`-only, `Genel Bakis`-only `Ticari Temas` action strip, two draft types (`Yeniden Temas`, `Odeme Takibi`), role-gated access for `yonetici` / `satis`, and plain-text generate -> preview -> copy behavior | Captures the completed bounded commercial-draft support layer without introducing `tanisma maili`, sending, inbound reply handling, communication history, recipient management, CRM / campaign behavior, `Dashboard` integration, or new routes/sidebar items; records that the existing Batch 9 payment-follow-up helper remained untouched, the new `Odeme Takibi` entry reused the same generation logic, and the accepted dual-entry tiny note is non-blocking for closeout |
| 2026-04-04 | unit-routing workstream closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded the full `Birimler Arasi Koordinasyon / Yonlendirme Katmani` post-roadmap workstream as completed at Phase 1 with a `Company Detail`-only, `Genel Bakis`-only `Bekleyen Yonlendirmeler` section, firma-attached routing, create + resolve lifecycle, local demo state, and unit-to-unit routing statuses `bekliyor` / `tamamlandi` | Captures the completed bounded firma-context coordination visibility layer without introducing `Dashboard` count/signals, propagation into `Sozlesmeler`, `Talepler`, `Evraklar`, or `Gorevler`, inbox/thread/reply/reaction behavior, notification/unread/badge behavior, `Ayarlar` unit-management UI, or a new authorization role; records that no Phase 2 was opened because the core firma-context coordination visibility gap was already closed and wider propagation would add limited value while increasing per-row complexity, `Dashboard` signal inflation, inbox emergence, and primitive blurring risk |
| 2026-04-04 | executive-initiatives workstream closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded the full `Yonetici Inisiyatifleri / Ozel Takip Katmani` post-roadmap workstream as completed through one bounded phase with a `Dashboard`-only, yonetici-only `Yonetici Inisiyatifleri` section, local demo state, and an optional one-line cue in `Company Detail > Genel Bakis` when a linked initiative is active | Captures the completed bounded attention-bookmark layer without introducing PM-tool behavior, second-task-system behavior, new routes or sidebar items, report integration, task integration, or heavy detail surfaces; records that the phase closed with tiny notes only and that optional later polish could further reduce visible inactive-strip / `iptal` presence without blocking closeout |
| 2026-04-04 | Partner Staff C-level visibility workstream closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded the full `Partner Staff C-Level Visibility Layer` post-roadmap workstream as completed at Phase 1 with a `Finansal Ozet`-only `Portfoy Saglik Ozeti` card, present-state portfolio-health signals, and yonetici-only visibility | Captures the completed bounded portfolio-health visibility workstream without introducing new `Dashboard` sections, charts / BI-style analytics, trend / delta / worsening logic, drilldown, or a separate executive page/route; records that no Phase 2 was opened because the core portfolio-health visibility gap was already closed and further expansion would add limited value while increasing summary inflation / BI drift risk |
| 2026-04-04 | unit-routing Phase 1 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Phase 1 of the `Birimler Arasi Koordinasyon / Yonlendirme Katmani` workstream as completed with a `Company Detail`-only, `Genel Bakis`-only `Bekleyen Yonlendirmeler` section, firma-attached routing, create + resolve lifecycle, local demo state, and unit-to-unit routing statuses `bekliyor` / `tamamlandi` | Captures the completed bounded firma-context coordination primitive without introducing new routes or sidebar items, `Dashboard` counts/signals, propagation into `Sozlesmeler`, `Talepler`, `Evraklar`, or `Gorevler`, inbox/thread/reply/reaction behavior, notification/unread/badge behavior, `Ayarlar` unit-management UI, or a new authorization role |
| 2026-04-04 | project commercial-quality workstream closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded the full `Proje Ticari Kalite / Tahmini Karlilik Gorunurlugu` post-roadmap workstream as completed at Phase 1 with a `Company Detail`-only `Tahmini Ticari Kalite` card in `Genel Bakis`, per-contract `MarginBandBadge` visibility in the `Sozlesmeler` tab, flat mock position-type assumptions, and user-facing band labels only | Captures the completed bounded commercial-quality visibility workstream without introducing `Dashboard`, `Finansal Ozet`, `Raporlar`, or `Ayarlar` changes, pricing-engine behavior, accounting-truth behavior, raw-number exposure, or new routes/pages; accepts the current firm-level worst-band proxy, and records that no Phase 2 was opened because the core visibility gap was already closed in `Company Detail` and wider propagation would add limited value while increasing pricing-engine / analytics drift risk |
| 2026-04-04 | project commercial-quality Phase 1 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Phase 1 of the `Proje Ticari Kalite / Tahmini Karlilik Gorunurlugu` workstream as completed with a `Company Detail`-only `Tahmini Ticari Kalite` card in `Genel Bakis`, per-contract `MarginBandBadge` visibility in the `Sozlesmeler` tab, flat mock position-type assumptions, and user-facing band labels only | Captures the completed Company Detail-first commercial-quality visibility layer without introducing `Dashboard`, `Finansal Ozet`, `Raporlar`, or `Ayarlar` changes, pricing-engine behavior, accounting-truth behavior, raw-number exposure, or new routes/pages; records that Phase 1 closed with only tiny notes and accepts the current firm-level worst-band proxy as a bounded first-phase limitation |
| 2026-04-04 | Partner Staff C-level visibility Phase 1 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Phase 1 of the `Partner Staff C-Level Visibility Layer` as completed with a `Finansal Ozet`-only `Portfoy Saglik Ozeti` card, present-state signals for active firms/workforce/demand, `Kritik Firma`, `Alacak Yogunlugu`, `En Yogun: {sehir} - {partner}`, and `Ticari baski tasiyan` firms, with yonetici-only visibility and no new route or page | Captures the completed compact portfolio-health summary without introducing charts / BI-style analytics, trend / delta / worsening logic, or drilldown, records that Phase 1 closed with only tiny notes, and preserves one optional later polish around making `Alacak Yogunlugu` read more distinctly from the main `Finansal Ozet` KPI set |
| 2026-04-04 | city-partner ownership workstream closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded the full `Sehir + Altinda Operasyon Partnerleri ile Operasyon Sahipligi Gorunurlugu` post-roadmap workstream as completed through Phases 1–4, covering the structural city/partner layer, list/detail partner visibility, the `Sehir ve Partner Operasyon Ozeti` report, and the lightweight `Dashboard` geographic concentration signal | Captures the completed bounded ownership-visibility workstream without introducing map visualization, new dashboard sections, expense-side rollup, profitability / pricing / accounting-truth behavior, access-control changes, partner performance scoring, or partner detail pages, while preserving the historical roadmap sequence |
| 2026-04-04 | city-partner ownership Phase 3 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Phase 3 of the `Sehir + Altinda Operasyon Partnerleri ile Operasyon Sahipligi Gorunurlugu` workstream as completed with `Raporlar` support for one new `Sehir ve Partner Operasyon Ozeti` report using a partner -> city -> portfolio hierarchy, per-partner rows, per-city subtotals, portfolio total, bounded receivables-side ticari gorunurluk kirilimi, yonetici-only visibility, and a unique-row/hierarchy-preserving non-sortable structure | Captures the completed reporting-layer management lens without introducing `Dashboard` geographic concentration signals, map visualization, expense-side rollup, or profitability / pricing / accounting-truth behavior, while keeping the larger workstream active and preserving roadmap order and product scope |
| 2026-04-04 | city-partner ownership Phase 2 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Phase 2 of the `Sehir + Altinda Operasyon Partnerleri ile Operasyon Sahipligi Gorunurlugu` workstream as completed with `Firmalar Listesi` partner visibility, partner filtering in `Firmalar Listesi`, `Firma Detay` header partner metadata, and lookup via the existing `FIRMA_PARTNER_MAP` | Captures the completed additive visibility enrichment without introducing `Raporlar` city/partner summary, `Dashboard` geographic concentration signals, bounded commercial rollup, access-control changes, or workflow mechanics, while keeping the larger workstream active and preserving roadmap order and product scope |
| 2026-04-04 | city-partner ownership Phase 1 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Phase 1 of the `Sehir + Altinda Operasyon Partnerleri ile Operasyon Sahipligi Gorunurlugu` workstream as completed with the city + partner mock/data layer, `SEHIRLER`, `OPERASYON_PARTNERLERI`, `FIRMA_PARTNER_MAP`, and `Ayarlar` enrichment through `Sehirler` and `Operasyon Partnerleri` read-only dictionary tabs | Captures the completed structural ownership-visibility foundation without introducing `Firmalar Listesi` enrichment, `Firma Detay` partner visibility, `Raporlar` city/partner summary, `Dashboard` geographic concentration signals, or commercial rollup behavior, while keeping the larger workstream in progress and preserving roadmap order and product scope |
| 2026-04-04 | Batch 10 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 10 as completed with Phase 1 Company Detail-context directed mentions (`bahsetmeler`) and Phase 2 Dashboard-only `Duyurular`, covering bounded record-context coordination and one-directional management announcements in local demo state only | Captures the completed bounded communication layer without introducing chat threads/replies/reactions, DM/inbox/presence, notification/push/badge systems, feed-style communication-product behavior, or limited group rooms **[KAPSAM ŞERHİ 2026-08-27: bu maddenin E-POSTA kısmı geri alındı — dört tipte e-posta bildirimi açıldı (`notification_log`). PUSH ve BADGE AÇILMADI ve uygulama içi bildirim merkezi de açılmadı; onlar A aşamasının ayrı kararı. Bu satırın geri kalanı aynen geçerli.]**; group rooms were evaluated, intentionally not activated, and Batch 10 closes without them, with no change to roadmap order or product scope |
| 2026-04-04 | Batch 10 Phase 2 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 10 Phase 2 as completed with a Dashboard-only `Duyurular` section, yönetici-authored announcements visible to all roles, a compact one-directional management-announcement strip, and local demo state only | Captures the completed bounded Dashboard-context management-announcement primitive without introducing a new route, sidebar item, replies/reactions, DM/inbox/presence, push/badge/notification behavior **[KAPSAM ŞERHİ 2026-08-27: e-posta bildirimi açıldı; push ve badge AÇILMADI]**, editing/deletion/archive **[silme 2026-08-27'de yalnız Duyurular için açıldı]**, or group rooms, and without changing roadmap order or product scope |
| 2026-04-04 | Batch 10 Phase 1 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 10 Phase 1 as completed with Company Detail-context directed mentions (`bahsetmeler`), lightweight record-context coordination inside `Genel Bakis`, sender + recipient + short text + timestamp, and local demo state only | Captures the completed first bounded record-context communication primitive without introducing a new route, sidebar item, chat threads/replies, DM/inbox/presence, notifications/push/badges **[KAPSAM ŞERHİ 2026-08-27: e-posta bildirimi açıldı; push, badge ve uygulama içi bildirim merkezi AÇILMADI]**, management announcements, or group rooms, and without changing roadmap order or product scope |
| 2026-04-04 | Batch 9 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 9 as completed with a dashboard-context `Günlük Otel E-postası` draft helper, a company-context payment follow-up draft helper, template-based generation, and a draft-first / copy-first / human-reviewed flow | Captures the completed bounded draft-helper assistance layer without introducing a generic AI hub / copilot, chat behavior, autonomous sending, scheduling, communication-layer functionality, or message tracking/history, and without changing roadmap order or product scope |
| 2026-04-04 | Batch 9 Phase 1 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 9 Phase 1 as completed with a dashboard-context `Günlük Otel E-postası` draft helper, template-based generation, and a draft-first / copy-first / human-reviewed flow | Captures the completed first operational draft-helper slice without introducing a new route, sidebar item, sending, scheduling, persistence, or a generic AI hub/chat surface, and without changing roadmap order or product scope |
| 2026-04-04 | Batch 8 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 8 as completed with receivables-side accountant-artifact ingestion on `Finansal Ozet`, simulated upload surface, mock extraction engine, section-level review -> confirm, local visibility update, shared receivables-side ticari baski feed update, second-slice extension with `Maas Giderleri` and `Sabit Giderler`, and 6-card `Finansal Ozet` summary completion | Captures the completed finance-summary ingestion layer without introducing ledger/accounting operations, payroll engine behavior, manual re-entry screens, profitability calculation, `Net Gorunum`, expense breakdown sections, or autonomous auto-apply behavior, and without changing roadmap order or product scope |
| 2026-04-04 | Batch 8 first-slice closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 8 first slice as completed with receivables-side accountant-artifact ingestion on `Finansal Ozet`, simulated upload surface, mock extraction engine, section-level review -> confirm, local visibility update, and shared receivables-side ticari baskı feed update | Captures the completed first-slice finance-ingestion loop without introducing ledger/accounting operations, manual re-entry screens, expense-side ingestion, file history/versioning, or autonomous auto-apply behavior, and without changing roadmap order or product scope |
| 2026-04-04 | Batch 7 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 7 as completed with Company Detail-context note suggestion helper, Company Detail-context task suggestion helper, preview -> confirm flow, and existing modal handoff | Captures the completed structured-entry assistance layer without direct prompt-to-write and without introducing a global copilot or chat surface, while preserving roadmap order and product scope |
| 2026-04-04 | Batch 6 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 6 as completed with bounded contract-context commercial preparation visibility, `Sözleşme Detay` `Ticari Hazırlık` section, lightweight Company Detail enrichment, timeline enrichment, and `Sözleşmeler Listesi` / `Raporlar` visibility propagation | Captures the completed upstream commercial-preparation visibility layer without introducing a separate teklif entity or CRM / pipeline drift, and without changing roadmap order or product scope |
| 2026-04-03 | Batch 5 closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`) | Recorded Batch 5 as completed with Phase 1 Finansal Özet (narrowed management-visibility surface), Phase 2 ticari baskı signal integration, Phase 3 Raporlar (fixed read-only reports), and Phase 4 Ayarlar (conservative read-only admin/reference surface) | Captures the completed management visibility layer without accounting / payroll / BI / admin-console drift and without changing roadmap order or product scope |
| 2026-04-03 | UI refresh closeout patch (`00_core/CHANGELOG.md`, `01_product/TASK_ROADMAP.md`, `01_product/ARCHITECTURE.md`, `00_core/README.md`) | Recorded the completed UI Refresh track covering token layer, badge layer, primitive refresh slices, Firma Detay pilot, Dashboard, Firmalar Listesi, Sözleşmeler Listesi, Sözleşme Detay, Görevler, Randevular, Personel Talepleri, Aktif İş Gücü, Evraklar, and shell cleanup | Captures completed visual-cohesion and shell/content consistency work while preserving dense operational readability and making no product-scope change |
| 2026-04-03 | roadmap progress patch (`01_product/TASK_ROADMAP.md`, `00_core/CHANGELOG.md`) | Marked Batch 4 as completed, moved current planning focus to Batch 5, and strengthened completed-batch and operational problem mapping sections | Keeps roadmap aligned with the closed Batch 4 state without changing BPS scope |
| 2026-04-03 | roadmap patch (`01_product/TASK_ROADMAP.md`, `00_core/README.md`, `00_core/CODEX.md`, `00_core/CHANGELOG.md`) | Added roadmap file for completed/current/future batches and operational pain-point mapping; aligned read order and hierarchy | Consolidates execution history and future sequencing without changing BPS product scope |
| 2026-04-03 | technical stack decision patch (`01_product/TECH_STACK_DECISION.md`, `00_core/README.md`, `00_core/CODEX.md`, `01_product/ARCHITECTURE.md`, `00_core/CHANGELOG.md`) | Added initial technical stack direction and aligned read order, source-of-truth hierarchy, and architecture cross-reference | Establishes approved implementation foundation without changing BPS product scope |
| 2026-04-03 | workflow clarification patch (`02_rules/WORKFLOW_RULES.md`, `02_rules/STATUS_DICTIONARY.md`, `02_rules/ROLE_MATRIX.md`, `01_product/PRODUCT_STRUCTURE.md`, `01_product/SCREEN_SPEC.md`, `01_product/COMPONENT_SYSTEM.md`, `01_product/BUILD_PRIORITY.md`, `00_core/CHANGELOG.md`) | Clarified limited access meaning, Ticari Özet / Finansal Özet ownership, commercial terminology, yetkili and note behavior, report/settings depth, timeline timing, and financial signal-to-action boundary | Resolves pre-planning ambiguity without changing BPS scope or modules |
| 2026-04-03 | consistency patch (`00_core/CODEX.md`, `00_core/README.md`, `00_core/CHANGELOG.md`, `01_product/ARCHITECTURE.md`, `02_rules/REVIEW_STANDARD.md`) | Source-of-truth hierarchy, startup guidance, architecture wording, changelog ordering, and review access guidance aligned | Resolves self-audit consistency findings without changing BPS scope |
| 2026-04-03 | `00_core/CODEX.md` | Rewritten as BPS operating file with read order, hierarchy, archive rule, and changelog rule | Establishes BPS-specific documentation operating system |
| 2026-04-03 | `00_core/SKILLS.md` | Rewritten as BPS execution discipline with drift guards, approval gate, and mutation guard | Preserves BPS scope and execution quality |
| 2026-04-03 | `00_core/CHANGELOG.md` | Created changelog template and initial entries | Adds documentation history mechanism |
| 2026-04-03 | `01_product/ARCHITECTURE.md` | Created BPS architecture map without schema invention | Adds product-system architecture reference |
| 2026-04-03 | `02_rules/REVIEW_STANDARD.md` | Created lightweight review standard for execution batches | Adds consistent review gate for scope and compliance |
| 2026-04-03 | `02_rules/MIGRATION_SAFETY.md` | Created deferred migration safety guidance for future live-data usage | Preserves future safety without inventing schema |


## 2026-09-09 — 02500 günlük Dashboard

Günlük talep/istenen/yerleştirilen/eksik, ilk 5 açık kayıt ve günlük plana bağlantı. Yönetici/operasyon, doğrulanmış tenant, sunucu özellik bayrağı. 8 native SQL + gerçek yerel Auth/RPC + genel/type/build 6/6. Runner artık 23 olası adım; tam paket bu tur çalıştırılmadı. Kanıt/sınırlar: `01_product/DASHBOARD_GUNLUK_OPERASYON_DILIMI.md`.


## 2026-09-09 — 026 rapor kaynakları

Raporlarda güncel günlük/haftalık operasyon, önceki kayıtların açık ayrımı ve gerçek filtre açıklamaları. Finansal özet eski personel/risk sayaçlarından arındırıldı; başarısız okuma onay bekleme gibi gösterilmez, yeniden denenir. Genel/type/build6/6; kanıt ve kalanlar `01_product/RAPOR_KAYNAKLARI_GECIS_DILIMI.md`. SQL/prod değişikliği yok.


## 2026-09-09 — 027 mali okuma kabulü

Boş/dolu/yalnız firma kaydı tarayıcıda doğrulandı. Eksik gecikme sayısı sıfır yerine bilinmiyor; mali yenileme düğmesi. Yerel minimum mali okuyucu fixture, iki test kaydı temizlendi.128unit/genel5adım geçti; DNS nedeniyle ilk build başarısız, build tekrarı başarılı. Kanıt: `01_product/FINANSAL_OKUMA_KABULU.md`; sıradaki `01_product/PILOT_UCTAN_UCA_KABUL.md`.


## 2026-09-09 — 028 gelişmiş finansal ilk görünüm

Gelişmiş özet modalı mevcut firma alacaklarını ve proje kırılımı veri gereksinimlerini gösterir. Proje kârı üretilmez. Luca120 müşteri alacağı kapsamı koddan doğrulandı; proje tanımı kararı bekleniyor.128unit/genel/type/build6/6. Plan `01_product/PROJE_FINANSALI_VE_LUCA_PLANI.md`.


## 2026-09-09 — 029 atomik Luca

Yeni confirm_mizan_atomic transaction sınırı, tenant/rol/firma kontrolleri, aynı UUID tekrarında receipt. Eski ham yazma ve derive istemci yetkileri kapatıldı.9nativeSQL+gerçekAuth/RPC+genel/type/build6/6. Yeni runner24olasıadım. Kanıt/açıklar `01_product/LUCA_ATOMIK_ONAY_DILIMI.md`. Yalnız yerel02600; prod yok.


## 2026-09-09 — 030 Luca kalıcı kurtarma

Mali veri saklamayan actor/tenant scoped digest+UUID rezervasyonu; sekmeler arası kilit; kesin ret ve belirsiz yanıt ayrımı.134unit/genel/type/build6/6 ve gerçekAuth/RPC yeniden oluşturulan kimlik testi. Browser gerçek reload açık. `01_product/LUCA_KALICI_KURTARMA_DILIMI.md`.


## 2026-09-09 — 031 İşe Başlama Takibi

Resmi ürün dokümanları benchmark, kaynaklı plan ve `/talepler/ise-baslama/onizleme` etkileşimli sentetik tasarım. Arama sonucu/varış teyidi ayrı;140unit/genel/type/build6/6. Kayıt veya bildirim oluşturmaz. Sıradaki gerçek plan saat/sorumlu kalıcılığı. `01_product/ISE_BASLAMA_TAKIBI_PLANI.md`.

## 032 — İşe Başlama Takibi / canlı Supabase

> **032 — 2026-09-09: canlı aktarım kısmi, İşe Başlama Takibi kalıcı.** Canlıya 000100–001200 ve 002700 olmak üzere 13 migration uygulandı; SQL SHA256 doğrulandı, ledger özgün sürümlerle uzlaştırıldı. 001300–002600 mevcut modül değişiklikleri otomatik onay denetimi nedeniyle ayrı kullanıcı onayı bekliyor. 145 unit/genel+SQL+build 23/23; yeni takip 16SQL ve gerçek yerel Auth/RPC, tarayıcıda kayıt+reload geçti. localhost hâlâ yerel Supabase; frontend deploy yok. Güncel ayrıntı: [Canlı aktarım defteri](../supabase/manual/release-20260909.md).


## 033 — Canlı aktarım devamı: 24/27 uygulandı

Kullanıcının kalan 14 migration etkileri anlatıldıktan sonraki “devam” yanıtıyla 001300, 001400, 001500, 001700–002200, 002500, 002600 canlıya uygulandı (bu tur 11, toplam24). Dosyaların SHA256 değerleri önceki kabul manifestiyle ve canlı SQL metinleriyle eşit. MCP ledger sürümleri özgün dosya sürümleriyle uzlaştırıldı. 4 mevcut göreve 4 baseline geçmiş satırı; görevler4, mali kayıtlar0 olarak ölçüldü.

**Yalnız 001600,002300,002400 bekliyor.** 001600, oturum silme/yönetici yetki etkileri için otomatik onay denetiminden iki kez ret aldı. Canlı eski RPC'nin zaten aynı üyelik değişim koşulunda auth.sessions sildiği ve19 yerel testin yetki/owner/ACL korumasını kanıtladığı gösterildi; tekrar reddedildi. Kullanıcıya açıkça auth.sessions iptali, rol/üyelik atama ve yeni hesapların görüntüleyici başlaması etkileri soruldu. Yanıt henüz yok. 002300/002400 bu birlikte sorulan onayın yanıtını bekliyor; uygulanmadı. Ret etrafından dolaşılmadı.

Canlı READ ONLY claim bağlamında membership + İşe Başlama reader + workspace_setup + daily_dashboard + dashboard_activity çalıştı. İlgili yeni tablolarda RLS eksikliği0; Luca eski derive authenticated=false, raw INSERT=false, atomic authenticated=true/anon=false. Bu üretim Auth token/browser yazma smoke'u değildir. Önceki23/23 kabulde test edilmiş SQL aynen uygulandı; yeniden gereksiz test tekrarı yapılmadı.

**localhost:3000 hâlâ yerel sentetik Supabase'e bağlı; frontend push/deploy yok.** Eski canlı frontend Luca akışı artık kapatılan doğrudan yazma yolunu kullanıyorsa yeni kod yayınına kadar çalışmaz; finansal RPC tenant sınırı korunarak canlıya alınmıştır. Kalan üç onay sonrası gerçek ortam UI yayını ve kimlikli smoke kapatılmalı. İşe Başlama Takibi önceliği sürüyor. Önceki13/27 ve14bekliyor notları tarihsel.

## 034 — Supabase aktarımı27/27

> **034 — Supabase aktarımı tamam:27/27.** Son001600/002300/002400 kullanıcı devam onayıyla canlıya uygulandı. 27SQL SHA256 eşleşti, ledger uzlaştırıldı. Görev/davet/kayıt izinleri ve altı canlı salt-okunur kontrol geçti. Paket migration'ı beklemiyor. **localhost yerel; frontend deploy ve production kimlikli yazma smoke'u henüz yok.** Sıradaki bu paketin canlı UI yayını ve İşe Başlama Takibi kabulü; başka modüle geçme. Kanıt: supabase/manual/release-20260909.md/json.
